# Жюри №4 — сырые отчёты судей (код 72a7ecd, 23.09.2026 17:06–17:14)

## Gatekeeper (Opus)
# Gatekeeper — Archibalt

- **Что это:** площадка, где AI в диалоге превращает сырое описание бизнес-проблемы в понятное ТЗ с рейтингом готовности 0–100 и публикует его в открытом каталоге задач для студенческих команд.
- **Для кого:** малый бизнес и организации без готового ТЗ (заказчики) и студенческие команды, которым нужны реальные задачи.
- **Что обещает:** из слабого запроса получается понятная задача (30 → 90), команды присылают решения, бизнес сам выбирает, с кем работать.
- **Вердикт:** `PASS`
- **Причина:** заголовок, подзаголовок и две кнопки («Обсудить задачу» / «Найти задачу») за 10 секунд объясняют двусторонний сценарий. Правая карточка 30 → 90 сразу показывает, что делает AI.

**Что мешает на первом экране:** непонятно, что значат «AI SANA» под логотипом, «готовность» и «#8 → #2» без подписи, а пользу для студентов (почему это Education) никто не называет. Кроме того, one-liner начинается с описания процесса, а не с ценности.

## Product Judge (Opus)
# Product Judge — Archibalt (HEAD 72a7ecd), живой стенд :8080, ai_mode openai

## Что проверено руками (17:06–17:12 по date)
Playwright 1280×720 (скрипт и скриншоты: scratchpad/jury4/pp/): главная → «Обсудить задачу» (чат без входа, открылся) → слабое описание «сеть кофеен, списания выпечки» → AI задал 5 вопросов (данные, результат, пользователи, критерии, контакт) с чипами-подсказками и «Пропустить −N к готовности» → счётчик «Карточка 6/10» → 2 варианта результата («Совет по выпечке на утро», срок «~2 недели · оценка AI», «Как проверим», «Что нужно от вас») → «Выбрать» → модалка «Войти как заявитель» → 000000 → «Собрать карточку» → /task/51/edit: 90/100 «приоритетная», 10/10 полей, подсказка «Контекст и потребность +10» → «Опубликовать» → /task/51, тост «Задача опубликована: место #3 из 11», концепт-картинка. Время от первого сообщения до публикации ~80 с.
curl: вход команды → отклик (201, все поля) → пустое idea → 400 «обязательные поля не заполнены: idea»; select токеном команды → отказ «войдите как заявитель»; select и confirm-stage токеном заявителя → OK, команда получила 10 баллов. Каталог GET /api/tasks отсортирован по score DESC.
Не проверял: чистый docker compose по README, PUT fields с пересчётом (чтобы не снимать задачу с каталога), UI отклика команды и UI выбора бизнеса (только API).

## Найденные дефекты (факты)
1. После успешного входа в чате остаётся розовый пузырь «Войдите как заявитель, чтобы продолжить. Повторить» (скрин pp/07concept.png) — коммит 5cb22bb заявляет, что это исправлено.
2. Чипы-подсказки к критериям успеха нерелевантны задаче про выпечку: «Заявки не теряются», «Обработка заявки быстрее вдвое» — шаблон, не AI.
3. Отрасль определилась как «Другое» при очевидном общепите.
4. Факт пользователя «пекарь решает на глаз, списываем ~15%, ~400 тыс. тенге в месяц» в карточку не попал; при этом рейтинг снял 10 баллов с «Контекст и потребность» с подсказкой «опишите, что происходит сейчас» — то есть пользователь это сказал, а система потеряла и затем попросила снова. (Частично моя вина: ответы шли не в порядке вопросов, но это реалистичное поведение пользователя.)
5. Концепт-картинка с английским текстом («Morning recommendation») в русскоязычном продукте.
6. Каталог живого стенда содержит мусор: «фвфыв» (22), «фвфовфыов» (19), отрасль «education» латиницей рядом с «Образование», префиксы «Демо:». Для демо на этом стенде — вредно.
7. Рост рейтинга (обязателен по §11) в пути openai не виден: карточка сразу приходит 90/100. Рост показан только статичным примером на лендинге (30→90) и счётчиком полей 0/10→6/10.
8. Первый вызов /api/auth/verify после request-code вернул не-JSON/ошибку, повторный — успех (не воспроизводил, отмечаю как нестабильность).

## Продуктовые оценки 0–10
1) Важность и конкретность проблемы — 7. Слабые бизнес-запросы → студенты не могут начать работу: проблема реальная и узнаваемая, пример библиотек/кофейни конкретен. Не хватает: ни одной цифры/интервью о масштабе проблемы у AI Sana/вузов. Правка: одна строка в README с фактом (сколько запросов «на глаз» приходит, сколько проектов срываются).
2) Соответствие кейсу Education (§2, §10–11) — 9. Все 8 шагов §2 реализованы и проходят (проверено UI+API), 10 полей, 7 весов §4 точно, уровни 0–39/40–69/70–89/90–100, ручной выбор, баллы за этап. Минус: §11 «показывают рост рейтинга» в живом openai-пути не виден (деф. 7). Правка: в редакторе показывать «было N (черновик) → стало M» и держать в демо шаг ручного дописывания поля с пересчётом.
3) Образовательный эффект — 5. Эффект косвенный: студент получает реальную задачу с данными, критериями и контактом; за подтверждённый этап +10. Нет обратной связи студенту по качеству отклика, нет траектории навыков, «этап» — одна кнопка. Правка: для команды показать, почему бизнес выбрал/отклонил (короткая причина) и баллы по этапам с критериями из карточки.
4) Ясность сценария — 7. Первый экран понятен за 10 с (H1, две кнопки), чат ведёт без входа, вход только при сборке. Минусы: висящий пузырь ошибки после входа (деф. 1), нерелевантные чипы (деф. 2), вход команды/выбор бизнеса в демо — отдельные переключения ролей. Правка: убрать пузырь после успешного входа; чипы генерировать из контекста или скрывать.
5) Отличие от обычного чата с LLM — 8. Детерминированный рейтинг в коде с причинами по каждому показателю и «+N» за дописанное, редактируемая карточка, варианты результата с «как проверим», публичный каталог, отклики, ручной выбор, ролевые права (403/401). Минус: сама беседа всё же похожа на анкету-чат; потеря сказанного факта (деф. 4) — слабость, типичная для LLM. Правка: перед сборкой показывать «что я записал» со всеми фактами пользователя.
6) Реалистичность внедрения — 5. Один docker compose, Postgres, mock без ключа — хорошо для пилота. Но: код 000000 для всех, сессии в памяти, рейтинг на regex (длинная бессмыслица = полный балл, признано в KNOWN_ISSUES), правка опубликованной карточки снимает её с каталога, нет модерации (мусор в каталоге, деф. 6). Способ поднять за оставшееся время: только почистить каталог демо-стенда; остальное — вне 5 часов.

## Рубрика TASK.md §9 (фактический балл)
| Критерий | Макс | Балл | Доказательство | Чего не хватает |
|---|---|---|---|---|
| Сквозной сценарий | 20 | 18 | Черновик→вопросы→карточка→публикация (#3 из 11)→отклик→select→confirm-stage прошли на живом стенде | Рост рейтинга не виден в openai-пути; UI отклика/выбора мной не прожат (только API) |
| Качество карточки | 15 | 12 | 5 уместных вопросов, 10/10 полей, редактор по разделам, фактов не выдумано | Нерелевантные чипы, «Другое», потерян факт о текущем процессе и 15% |
| Геймификация бизнеса | 25 | 21 | breakdown с причиной на каждый показатель («есть число с единицей (8%)»), missing с gain, место в каталоге в тосте, сортировка по score | Пересчёт после правки мной не проверен; regex-оценка обманывается; нет видимого «до→после» в живом пути |
| Каталог и отклики | 15 | 13 | Все published видны без входа, сортировка, фильтры industry/level в контракте, валидация отклика 400, select только заявитель | Мусорные задачи и «education» латиницей в каталоге демо-стенда |
| AI функция | 10 | 8 | Вопросы по недостающим полям, варианты результата с проверкой, карточка без выдумок, Guard и mock-фолбэк заявлены | Потеря факта пользователя, английский текст в концепте, шаблонные чипы |
| Техническое качество | 10 | 8 | Коды 400/401/403, права ролей, README с формулой, env, архитектурой, тестами | Чистый запуск по README мной не проверен; разовая ошибка verify |
| Демонстрация | 5 | 3 | demo/script.md подробный, маршрут отрепетирован частично | Сам сценарий пишет «не PASS по времени», GPT Image не входил в проверенный прогон, полного повтора нет |
| **Итого** | 100 | **83** | | |

## Минимальные правки до сдачи (по убыванию пользы)
1. Демо вести на чистом стенде (или удалить мусорные задачи 17, 47 и привести «education» к «Образование») — деф. 6.
2. Скрывать пузырь «Войдите как заявитель…» после успешного входа — деф. 1.
3. В демо показать рост: собрать карточку, в редакторе дописать поле «Контекст/потребность» → 90→100 и смену места; либо показывать в редакторе «черновик N → сейчас M».
4. Скрыть статичные чипы для критериев успеха, если они не из контекста.

## Technical Judge (Opus)
# Technical Judge: Archibalt (HEAD 72a7ecd), 23.09.2026 17:06–17:11

## Шаг 1. Запуск по README (факт)
- Клонировал с GitHub (сеть была) в /tmp/jury4-clone: 219 коммитов, HEAD 72a7ecd.
- Изменил только порты, потому что 8080 и 5432 заняты dev-стендом. Override: `db.ports: !override []`, `app.ports: 127.0.0.1:18096:8080`. Запуск `docker compose -p jury4 ... up --build -d`, OPENAI_API_KEY не задан.
- Сборка прошла с первого раза. `/api/health` → `{"ai_mode":"mock","db":true,"ok":true}`. В логе: `seed загружен teams=5 tasks=5 proposals=5`, `рейтинги пересчитаны`.
- Сценарий через curl (API_CONTRACT) прошёл полностью:
  - каталог: 5 задач 90/70/60/42/30, все 4 уровня, сортировка по убыванию;
  - анонимный `POST /tasks` (dynamic), затем next-question. На «здравствуйте» агент переспросил про данные другими словами. После 3 содержательных ответов `done` с причиной, preview 10→30→45→50;
  - result-options: 2 варианта;
  - `answers` без входа → 401. Вход `000000` → `POST /owner {}` → карточка 50 → apply-result 65 → visual (mock SVG) → PUT fields 70 «ready» (previous_score 65) → confirm: published, rank 3;
  - две команды откликнулись. Отклик без ссылки → 400. Команда пытается сделать select → 401. Бизнес: select / reject / confirm-stage → команде +10 очков. Пустой черновик → 400, `/tasks/999` → 404.
- Playwright без входа, headless, 1280×800: главная (H1 совпадает) → «Обсудить задачу» → 4 ответа → варианты → «Выбрать» → модалка «Войти как заявитель» → «Получить код» → 000000 → «Вы вошли как заявитель», mock-концепт в чате → «Собрать карточку» → /task/9/edit → «Подтвердить и опубликовать» → /task/9, «Задача опубликована: место #4 из 7». Заняло около 20 с. В консоли одна ошибка 401: ожидаемый ответ до входа.
- Отклик команды, выбор и подтверждение этапа в браузере НЕ прогонял, не хватило времени. Эти шаги проверены только через API.
- Стенд удалён: `down -v`, клон удалён. Dev-стенд :8080 не трогал.

## Шаг 2. Сборка и код
- `go vet ./...`: чисто. `go test ./...`: ai, http, rating, store все ok. `npm ci && npm run build`: ok (JS 302 kB, 94 kB gzip).
- `internal/ai/openai.go`: Responses API без SDK, `strict: true`, клиент с таймаутом 10 с. Отдельная быстрая модель для вопросов и вариантов.
- `ai.go run[T]`: 2 попытки primary, затем mock. `lastErr` виден в `/api/ai`.
- `guard.go`: пересечение стемов не меньше 60%, числа только в паре с тем же словом, имена собственные, полярность отрицания. README утверждает, что отрицание «не ловится», а в коде проверка полярности есть: README занижает возможности.
- `image.go`: gpt-image-1, quality low, таймаут 60 с. Без ключа SVG-заглушка с пометкой mock.
- `internal/rating`: 7 весов 20/20/15/15/10/10/10, правила 0 / половина / полный вес. С README совпадает. Ключевые слова вместо смысла команда признаёт сама.
- `internal/http/handler.go`: `anonDraft = owner=="" && status==clarifying`. `ownerOrAnon` пускает без входа только диалог. `setOwner` присваивает анонимную задачу ЛЮБОМУ вошедшему. `getTask` отдаёт задачу в любом статусе без авторизации, контакт маскируется.

## Шаг 3. Оценки 0–10
1. **Агентное поведение: 6.** Агент задаёт вопросы по одному и выбирает следующий по полям, которых не хватает. На пустой ответ переспрашивает (проверено: «здравствуйте» → «Давайте иначе…»). Останавливается сам с причиной, границы 3–5 и 8 задаёт код. Сам предлагает варианты результата и после выбора сам рисует концепт. Но это жёстко заданный конвейер: нет инструментов, нет планирования, нет действий за пределами вопросов и ответов. Минимальная правка: пусть агент проверяет собранную карточку по рейтингу и сам задаёт один добивающий вопрос по полю с наибольшим `gain`.
2. **Необходимость и качество OpenAI API: 6.** Интеграция грамотная: Responses API, strict-схема, две модели, gpt-image-1, повтор и откат на mock. Но весь продукт работает и без OpenAI: mock детерминирован и проходит весь путь. Живой вызов я не видел, доказательство только `docs/ai-live-sample.json`, демо тоже репетировали на mock. Выходит, API нужен ради качества вопросов, но без него ничего не ломается. Минимальная правка: в README таблица «mock или OpenAI» на одном черновике, в демо показать `ai_mode: openai`.
3. **Сквозная работоспособность: 8.** Чистый клон поднимается одной командой, seed загружается, весь путь §11 проходит (API полностью, UI до публикации). Минус 2: отклик и решение в UI я лично не проверял. Заголовок задачи — обрезанный черновик («Нужно приложение для учёта заявок, сейчас»).
4. **Надёжность и проверка результата: 7.** Тесты и vet зелёные, есть валидация 400/401/403/404, повтор и откат, Guard, границы в коде. Дефекты:
   - а) шум попадает в карточку: при сборке из mock получилось `data = "здравствуйте Выгрузка заявок из Excel…"`. Коммит 5cb22bb исправил это только для вариантов результата;
   - б) Guard лексический и не проверяет тип поля: в `contact` попал текст «Хотим прототип формы и таблицы заявок» (в mock ответ кладётся в поле вопроса);
   - в) варианты для apply-result хранятся в памяти процесса и теряются при рестарте.

   Минимальная правка: фильтр шума из `rating/noise.go` применять и к ответам при сборке карточки, а для `contact` требовать email или телефон.
5. **Безопасность и приватность: 4.** Все три проблемы воспроизведены:
   - а) **захват анонимного черновика.** `evil@x.kz` вошёл кодом 000000 и сделал `POST /tasks/8/owner {}` на чужой анонимный черновик. Задача стала его, текст черновика тоже виден ему. ID идут подряд, так что перебор тривиален;
   - б) **неопубликованные черновики читаются публично по id.** `GET /api/tasks/6` без токена отдал `draft_text` в статусе clarifying. То же для любых неопубликованных задач заявителей;
   - в) **`GET /api/ai → last_call` публично отдаёт черновик и ответы последнего пользователя**: утечка между сессиями.

   Плюсы: контакты маскируются, решения по откликам проверяют владельца (команда получает 401/403), ключ не в репо, .env в .gitignore. Код 000000 в демо допустим, это признано. Минимальная правка: при анонимном создании выдавать `draft_secret` (cookie или токен) и требовать его для next-question, result-options и owner. `getTask` для неопубликованной задачи отдавать только владельцу или по `draft_secret`. В `last_call` убрать draft и answers либо закрыть флагом.
6. **Техническая честность демо: 7.** Сценарий прямо запрещает рисовать экраны, требует называть `ai_mode` и не обещает абсолютной защиты от выдумок. Минусы:
   - прогон отмечен на `d39931f`, а не на финальном `5cb22bb`, и в mock;
   - в README противоречие: раздел «Архитектура» говорит «без входа бизнеса задачу не создать», а сценарий и код создают анонимно;
   - слоган «**ничего не выдумывает**» сильнее, чем лексический Guard;
   - в «не делали» указана «мобильная адаптация», а в коммитах есть «адаптивный диалог» (мелочь).

## Шаг 4. Чеклист техотбора (отдельно от баллов)
- README по п. 5.4.15:
  - описание ✓, архитектура ✓, технологии ✓, установка ✓, запуск ✓;
  - зависимости ✓ (Docker; Go 1.25+, Node 22+, PG16);
  - окружение ✓ (таблица и `.env.example`);
  - пошаговая проверка ✓ (9 шагов с ожидаемым результатом);
  - сторонние компоненты и лицензии ✓. Транзитивные npm-зависимости не перечислены, это допустимо.
- Допуск:
  - запуск по инструкции ✓ (менял только порты из-за нашей машины);
  - история коммитов ✓: 219 коммитов, каждый час 13–17 (53/59/49/38/3). 16 коммитов до 13:00 содержат только правила и документы, файлов cmd/internal/web/db нет;
  - компоненты и лицензии раскрыты ✓;
  - личные аккаунты не нужны ✓ (mock без ключа).
- §10 TASK.md: репозиторий ✓, MVP запускается ✓, README с архитектурой, формулой, правилами каталога и сценариями ✓, сценарий демо ≤5 мин ✓.
- Вне технической части, для Gatekeeper: в ТЗ «Команда 3–5 участников», а в README указаны двое.
- **Итог техотбора: ПРОЙДЕН** (с оговоркой о составе команды для Gatekeeper).

## Шаг 5. Рубрика TASK.md §9 (техническая сторона)
| Критерий | Балл | Доказательство | Чего не хватает |
|---|---|---|---|
| Сквозной сценарий (20) | 18 | curl: черновик → вопросы → карточка → публикация → 2 отклика → select/reject → stage; UI до публикации (#4 из 7) | Отклик и решение не проверены мной в UI |
| Качество карточки (15) | 11 | Вопросы по пробелам, переспрос на шум, редактор, 10 полей, пустое остаётся пустым | Шум «здравствуйте» в `data`; не тот тип в `contact`; title — обрезанный черновик |
| Геймификация бизнеса (25) | 21 | breakdown по 7 показателям, missing с gain, пересчёт 50→65→70, rank / rank_if_confirmed, уровни и сортировка | Ключевые слова: длинная бессмыслица получает полный балл (признано), рейтинг можно накрутить |
| Каталог и отклики (15) | 14 | Только published, фильтры, отклик без лимита, ручной select/reject, команда не может выбирать (401) | Неопубликованные задачи читаются по id (утечка, не функция) |
| AI функция (10) | 7 | Strict JSON, Guard, откат, промпты открыты на `/api/ai` | Живой OpenAI не подтверждён запуском; шум в карточке |
| Техническое качество (10) | 7 | Одна команда запуска, тесты, vet, валидация, структура | Захват анонимного черновика, публичный last_call |
| Демонстрация (5) | 4 | Сценарий ≤5 мин с чекпоинтами | Репетиция не на финальном SHA и в mock |
| **Итого** | **82/100** | | |

Решение Technical Judge: техотбор пройден. Балл 82 — не 100. Блокеры для роста по порядку: 1) приватность анонимного черновика, чтения по id и `last_call` (около 30 строк кода); 2) шум в полях карточки; 3) демо на финальном SHA с `ai_mode: openai`.

## Red Team Judge (Astra)
**Вердикт Red Team: не рекомендую пропускать этот срез без исправлений.** Проверен HEAD `72a7ecd`. Ниже — выводы по коду и материалам: GET к `localhost:8080` из моей среды не прошёл, браузер недоступен. Это **не доказательство неработоспособности стенда**; успешный живой прогон я также не засчитываю.

**1. Основания для отклонения**

1. **Чужой анонимный черновик можно присвоить и испортить.**  
   **Наблюдение:** любой вошедший бизнес присваивает задачу по id; доказательства создания черновика нет — [handler.go:752](/Users/abylay/alemhack/internal/http/handler.go:752). До присвоения разрешены анонимные изменения диалога — [handler.go:711](/Users/abylay/alemhack/internal/http/handler.go:711). Запись владельца безусловная, поэтому возможна и гонка — [auth.go:128](/Users/abylay/alemhack/internal/store/auth.go:128).  
   **Почему отказ:** чужой пользователь может перехватить обязательный путь от черновика до решения бизнеса. Это дефект технического качества и сквозного сценария.  
   **Минимальная правка:** случайный секрет черновика для чтения, ответов и присвоения; атомарное присвоение только ещё бесхозной задачи.

2. **Неопубликованные сведения доступны посторонним двумя способами.**  
   **Наблюдение:** `GET /api/tasks/{id}` возвращает любой статус без авторизации; маскируется только `owner_contact`, но остаются исходник, ответы и поля карточки — [handler.go:363](/Users/abylay/alemhack/internal/http/handler.go:363). Публичный `/api/ai` дополнительно раскрывает последнюю чужую беседу; маскирование email и телефонов не скрывает содержание бизнеса — [guard.go:245](/Users/abylay/alemhack/internal/ai/guard.go:245).  
   **Почему отказ:** ручное подтверждение публикации не обеспечивает приватности черновика. Реальные данные здесь использовать нельзя без раскрытия посторонним.  
   **Минимальная правка:** неопубликованные задачи отдавать владельцу или обладателю секрета черновика; публичную AI-трассу заменить фиксированным синтетическим примером.

3. **Оплачиваемую генерацию можно вызывать без установленного предела.**  
   **Наблюдение:** картинки требуют владельца — прямого анонимного вызова **нет**. Однако вход доступен по произвольному контакту и общему коду — [auth.go:274](/Users/abylay/alemhack/internal/http/auth.go:274). Каждый повтор `/visual` заново вызывает OpenAI; серверных квот, ограничения параллелизма и использования сохранённого результата нет — [visual.go:57](/Users/abylay/alemhack/internal/http/visual.go:57). Текстовые AI-вызовы доступны и анонимно.  
   **Почему отказ:** посетитель способен расходовать общий API-бюджет и сорвать демонстрацию. Для Demo Day это также провал доказательства масштабируемости.  
   **Минимальная правка:** серверный общий бюджет, лимит частоты и параллельных генераций; повторная выдача сохранённого концепта по умолчанию.

4. **Анонимный разговор теряется для пользователя после закрытия.**  
   **Наблюдение:** id существует в памяти компонента, локальный исходник очищается после создания — [AgentChat.tsx:175](/Users/abylay/alemhack/web/src/components/AgentChat/AgentChat.tsx:175). При закрытии обещано: «Задача останется в разделе “Мои задачи”» — [AgentChat.tsx:413](/Users/abylay/alemhack/web/src/components/AgentChat/AgentChat.tsx:413). Но этот список исключает задачи без владельца — [auth.go:84](/Users/abylay/alemhack/internal/store/auth.go:84).  
   **Почему отказ:** обычное закрытие окна обрывает новый основной сценарий вопреки обещанию сохранения.  
   **Минимальная правка:** сохранять id и секрет анонимного черновика, предлагать продолжение при повторном открытии.

5. **«Переспрашивает непонятное» работает лишь для ограниченного словаря.**  
   **Наблюдение:** любой ответ, не распознанный как шум, закрывает поле — [mock.go:178](/Users/abylay/alemhack/internal/ai/mock.go:178). Ответ «Сегодня хорошая погода» на вопрос о данных не отсекается правилами [noise.go:23](/Users/abylay/alemhack/internal/rating/noise.go:23). Кроме того, длина исходника ≥60 символов автоматически закрывает контекст и потребность; после пяти вопросов код может сообщить «ключевых сведений достаточно» независимо от пробелов — [mock.go:239](/Users/abylay/alemhack/internal/ai/mock.go:239).  
   **Почему отказ:** количество вопросов подменяет их уместность и качество карточки — прямой риск для §3 и 15 баллов рубрики.  
   **Минимальная правка:** не закрывать поле только по отсутствию слов-заглушек или длине текста; разрешить модели повторное уточнение. При достижении лимита честно показывать оставшиеся пробелы.

6. **Гарантия «ничего не выдумывает» неверна.**  
   **Наблюдение:** гарантия дана в [README.md:5](/Users/abylay/alemhack/README.md:5), но для `context` и `need` проверка отрицаний отключена — [guard.go:32](/Users/abylay/alemhack/internal/ai/guard.go:32). Контрпример по коду: исходник «Мы **не** передадим таблицу продаж студентам» допускает поле контекста «Мы передадим таблицу продаж студентам». Совпадение значимых слов сохранено. Это контрпример защиты, **не наблюдавшийся ответ OpenAI**.  
   **Почему отказ:** §5 запрещает добавлять факты; ручное подтверждение не превращает недостаточную проверку в заявленную гарантию.  
   **Минимальная правка:** для таких полей сохранять исходные формулировки либо показывать проверяемые цитаты; убрать абсолютную гарантию и добавить проверку этого контрпримера.

7. **Рейтинг награждает видимость конкретики.**  
   **Наблюдение:** из ветки расчёта следует, что `success_criteria = "Проверим как получится"` получает полный вес **15/15**: присутствует «проверим», есть три разных слова — [rating.go:383](/Users/abylay/alemhack/internal/rating/rating.go:383). Условия успешной приёмки здесь нет.  
   **Почему отказ:** это бьёт по главному критерию — геймификации бизнеса, **25 баллов**. Позиция растёт за текстовый триггер, а не полезное дополнение.  
   **Минимальная правка:** полный балл давать за конкретный объект проверки и условие прохождения; расплывчатую формулировку оценивать частично. Закрепить контрпример тестом.

8. **Целостная демонстрация текущей версии не доказана.**  
   **Наблюдение:** [demo/script.md:3](/Users/abylay/alemhack/demo/script.md:3) прямо описывает проверки разных этапов на разных коммитах, отсутствие полного повторения и замера; GPT Image в прогон не входил. На главной «стало» вычисляется суммированием возможных прибавок, а не чтением сохранённого результата — [BeforeAfter.tsx:28](/Users/abylay/alemhack/web/src/components/BeforeAfter.tsx:28).  
   **Почему отказ:** план показа и расчётная иллюстрация не доказывают §11, сквозной сценарий на 20 баллов и демонстрацию на 5.  
   **Минимальная правка:** один непрерывный прогон текущего HEAD ≤5 минут с двумя ролями и всеми переходами; иллюстрацию подписать «возможный результат».

**2. Точная формулировка кейса и §10–11**

- **§10:** исходники, архитектура, формула, правила каталога и сценарии представлены. Но инструкция противоречива: [README.md:65](/Users/abylay/alemhack/README.md:65) требует входа до создания, ниже описан анонимный старт. Это замечание также к п. 5.4.15. Запуск и готовая демонстрация текущей версии независимо не подтверждены.
- **§11:** сценарный план содержит требуемые переходы. Их выполнение на текущем HEAD не доказано; утверждать, что переходы заведомо отсутствуют, оснований нет. Подготовленный пример разрешён §10, ручное подтверждение и выбор бизнеса обязательны — это не скрытый оператор.
- **Состав команды:** [TASK.md:16](/Users/abylay/alemhack/TASK.md:16) требует 3–5 участников, [README.md:7](/Users/abylay/alemhack/README.md:7) перечисляет двоих. В `KNOWN_ISSUES.md` заявлен конфликт с общим Положением, но решения организатора в материалах нет. При заданном приоритете TASK соответствие не подтверждено.
- **OpenAI:** вызовы реализованы. README называет интеграцию необязательной, тогда как условия аудита требуют OpenAI. Разрешённый §5 mock обеспечивает запасной путь, но сам не доказывает использование OpenAI на сдаваемой сборке.
- Минимум данных §6 соблюдён: по пять черновиков, карточек, команд и откликов.

**3. UNPROVEN CLAIMS**

- **Эффект:** рост собственного балла и расчётного места не доказывает улучшения качества ТЗ или успешности работы студентов. Представленные данные синтетические.
- **Автономность:** скрытого человека не обнаружено. Но переспрашивание частично обеспечено заготовками и правилами; самостоятельная оценка достаточности сведений не доказана.
- **Преимущество перед аналогами:** весь продукт нельзя честно назвать одной обёрткой над промптом — есть каталог, хранение, рейтинг и решения бизнеса. Однако преимущество AI-части не показано. У **Riipen уже описана генерация проектов из целей работодателя через AI** в инструкции от 16.04.2026. Это прямой вопрос к инновационности Demo Day, не самостоятельное нарушение техотбора. [Riipen: создание проекта](https://help.riipen.com/en/articles/7336872-creating-a-project).
- **Картинки:** подпись «Концепт для обсуждения, не обещание объёма» честная; mock помечен. Прямой GET картинки неопубликованной задачи проверяет владельца. Ошибка генерации по коду не блокирует карточку. **Реальная задержка, стоимость, качество GPT Image и поведение при сбое на текущем стенде не подтверждены.**
