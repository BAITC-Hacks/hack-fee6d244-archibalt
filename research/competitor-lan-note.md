# Заметка: соседний проект по тому же ТЗ (23.09.2026, 14:52)

Источник: публичный dev-сервер команды в сети площадки, http://10.19.24.58:5173 (заголовок «Alem — ideas into impact»), только чтение исходников фронта, ничего не менялось.

## Факты
- React без TypeScript, весь UI в одном `App.jsx` (87 КБ), данные в `data.js` (18 КБ) и `localStorage`.
- В коде **0** вызовов `fetch`/`/api/`, **0** упоминаний OpenAI/GPT — бэкенда и живого AI нет, «вопросы» и «рейтинг» считаются на клиенте по статике.
- **0** упоминаний чата, входа, WebSocket.
- Есть: каталог, отклики, выбор — тот же сценарий ТЗ.

## Вывод
Сценарий ТЗ у всех одинаковый. Наши реальные отличия — живой OpenAI со строгой схемой и Guard против выдумок, детерминированная формула с защитой от накрутки, вход по коду, принятие с двух сторон и чат, Postgres, тесты, запуск одной командой. Ни одно из них не видно на первом экране.

## Что делаем (до 17:30)
1. Первый экран: «до/после» одной задачи с живым счётчиком и лестницей мест.
2. На карточке: пометка «из ваших слов» у полей и плашка «AI не добавил фактов; вырезано: …» из `last_call`.
3. Вход-модалка, «Мои отклики/задачи», принятие командой, чат — в UI.
4. В демо и питче проговорить: «живой OpenAI, а не заглушка», «формулу нельзя накрутить — покажем», «команда подтверждает проект».
