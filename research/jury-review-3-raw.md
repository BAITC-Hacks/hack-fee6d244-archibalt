# Жюри №3 — сырые отчёты судей (код 66c4fa2, 23.09.2026 15:45–15:59)

## Gatekeeper (Opus)
Вердикт: PASS (с оговорками).
Что это: сырое описание бизнес-задачи → карточка; AI задаёт 3–4 вопроса, оценка 0–100, публикация в открытом каталоге.
Для кого: бизнес, который пишет задачу, и студенческие команды, которые откликаются.
Результат: из расплывчатого запроса — задача с высокой оценкой и местом в каталоге (30 → 90, #5 → #1); команды откликаются сами, бизнес выбирает.
Причина PASS: заголовок «Сырой запрос → задача с оценкой 0–100», карточка «Было 30 / Стало 90» и живой каталог объясняют механику за 3–5 с; демо сразу с действия.
Мешает за 10 с: в заголовке нет слов «бизнес»/«студенты», «Команды выбирают сами» двусмысленно; связь с Education не видна («AI SANA» мелко); пример в поле про языковой центр, на карточке — про библиотеки; число вопросов расходится (3–4 / минимум три / 5 в демо); кнопка «Продолжить» под полем не видна.

## Product Judge (Opus)
# Product Judge: Archibalt (AI Sana Challenge Hub), HEAD 66c4fa2

Что проверено: README, TASK.md (§2–§11), HACKATHON_RULES.md (п. 5.7.2), demo/script.md, KNOWN_ISSUES.md, API_CONTRACT.md. Живой стенд :8080 в режиме `ai_mode: openai`.
- **Через API** пройден полный путь: задача #10 «Причины ухода первокурсников» — черновик → 4 вопроса → карточка 37 → варианты результата → 52 → правка полей 87 → confirm → место #2 из 6 → два отклика → select и reject → confirm-stage, у команды +10.
- **Через браузер (Playwright)** пройдено трижды: вход бизнеса по коду 000000 → диалог с агентом → варианты результата → «Собрать карточку» → редактор → публикация (задача #16, 54 балла, место #5 из 7). Затем экран команды, отклик и «Мои задачи» заявителя с кнопками «Выбрать / Отложить / Отклонить».
- Скриншоты лежат в jury3/: u_*, v_*, w_*, t_*, b_*, p_*.

## Блок A. Продуктовые критерии

| # | Критерий | Балл |
|---|---|---|
| 1 | Важность и конкретность проблемы | 6 |
| 2 | Соответствие Education-кейсу (TASK.md) | 8 |
| 3 | Образовательный эффект | 4 |
| 4 | Ясность пользовательского сценария | 7 |
| 5 | Отличие от обычного чата с LLM | 7 |
| 6 | Реалистичность внедрения | 5 |

### 1. Важность и конкретность проблемы — 6/10
**Доказательства.** Проблема взята из ТЗ: бизнес пишет слабый запрос, студенты не могут начать работу. Первый экран понятен за 10 секунд: «Сырой запрос → задача с оценкой 0–100. Команды выбирают сами» (home.png). Но README не приводит ни одного факта о проблеме. Нет числа, реального примера слабого запроса AI Sana и указания, сколько времени или откликов теряется сейчас. Все примеры синтетические, в названиях «Демо: …». Все команды на этом ТЗ получают одну и ту же проблему, и жюри не увидит, чем её понимание у этой команды глубже.
**Минимальная правка.** Добавить в README после первого абзаца 2–3 строки «Почему это важно»:
- один типичный слабый запрос в том виде, как его пишет бизнес;
- что из-за него теряет студент (не понимает данные, критерий приёмки, контакт);
- метрика продукта: «от черновика до карточки ≥70 за N минут», измеренная на своих 5 seed-черновиках.

### 2. Соответствие Education-кейсу — 8/10
**Доказательства.**
- Все 8 шагов §2 работают, проверено по API и в UI (см. шапку).
- §3: 10 полей карточки, веса §4 совпадают один в один (README, таблица «Формула рейтинга»).
- 4 уровня готовности; фильтры `industry` и `level`, `/api/tasks?level=draft` отдаёт задачу с 30 баллами.
- Число откликов не ограничено, решение только ручное: select, reject, hold.
- Seed §6: 5 черновиков, 5 карточек, 5 команд, 5 откликов (seed/*.json).
- §5: AI не ставит баллы и не выбирает команды.

**Минусы.**
- §11 не подтверждён артефактом: demo/script.md помечен «draft», нет засечённого прогона с озвучкой и нет ролика.
- Шаг 8 «после подтверждённого этапа» можно пройти до того, как команда приняла проект. Экран задачи #10 одновременно показывает «Выбрана, ждём ответа команды» и «Этап подтверждён». Баллы начислены за этап, который команда ещё не приняла.
- «Фильтр по теме» из ТЗ сделан как отрасль (допустимо).

**Минимальная правка.**
1. В `confirm-stage` разрешать действие только из статуса `accepted`, а не `selected`: одна проверка статуса.
2. Записать и положить в репо прогон §11 на живом OpenAI с засечённым временем.

### 3. Образовательный эффект — 4/10
**Доказательства.** Студент получает реальную задачу, где явно указаны данные, результат и критерий приёмки. Это полезно, но эффект косвенный. В продукте нет ничего, что учит команду.
- При отклонении отклика команда видит только «Отклонена» (экран задачи #10), причины нет.
- Баллы за этап фиксированные, +10, и не зависят от качества работы.
- Рекомендации не учитывают навыки: `GET /api/teams/4/recommended` для «Bilim UX» (интерес «образование») вернул все 5 задач уровня «рабочая» и выше, в порядке рейтинга, включая логистику и вторсырьё.
- Уровня сложности задачи или нужных навыков для студента в карточке нет.

ТЗ ставит геймификацию на бизнес, поэтому низкий балл здесь частично заложен самим кейсом. Но перегруженное жюри трека Education это заметит.
**Минимальная правка.** Одно необязательное поле «почему не выбрали» в reject, видимое команде в «Моих откликах». Это прямая учебная обратная связь без расширения объёма.

### 4. Ясность пользовательского сценария — 7/10
**Доказательства (плюсы).**
- Шаги подписаны: «ШАГ 1 ИЗ 3».
- Справа живая карточка с растущим предварительным баллом (u_03_agent.png).
- У пустых полей подсказки «+10 к готовности», у кнопки «Пропустить» — «−10 к готовности».
- Указана строка места: «Сейчас 87 · место #2 из 6 · до уровня «приоритетная» не хватает 3».

**Доказательства (минусы, наблюдались).**
- **Обещание и поведение расходятся.** Агент пишет «Задам 3–4 вопроса», затем задаёт 5 и сообщает «Задано максимальное число вопросов». На лендинге «AI задаёт 3–4 вопроса», в README «3–5».
- **Слабые варианты ответа.** В первом прогоне на первый вопрос «Кто обращается и что уже известно?» чипы-ответы повторяли сам вопрос: «Кто обращается и что уже известно», «Почему важно именно сейчас» (u_03_agent.png). У вопроса о контакте чип «Имя, должность, почта» — это шаблон, а не ответ.
- **Ошибка на экране «Мои задачи».** Опубликованная задача #16 (`status: published`, `confirmed: true`) показана как «НЕ ОПУБЛИКОВАНА». Причина: `/api/business/me` не отдаёт `rank`, а web/src/pages/MyTasks.tsx:41 выводит «не опубликована» при пустом `rank` (b_02.txt). Это экран шага 7, который по README показывают жюри.
- Переключатель «Я команда» в каталоге сразу открывает модалку входа, хотя смотреть каталог можно без входа (t_01_catalog_team.png).
- Статусы «Выбрана, ждём ответа команды» и «Этап подтверждён» стоят рядом (см. п. 2).

**Минимальная правка.**
1. MyTasks.tsx:41: условие по `task.confirmed` (или `status === 'published'`), а не по `rank`.
2. Текст агента «Задам 3–5 вопросов» и такой же текст на лендинге.
3. Чипы, совпадающие с текстом вопроса, отбрасывать.

### 5. Отличие от обычного чата с LLM — 7/10
**Доказательства (плюсы).**
- Балл считает детерминированный код, а не модель (internal/rating).
- Место в каталоге, отклики и ручной выбор — это продуктовая механика, не диалог.
- Guard вырезает из карточки факты, которых не было в тексте пользователя. Сырой ответ и `removed_by_guard` видны в `GET /api/ai`.
- Агент в пошаговом режиме выбирает следующий вопрос по недостающим полям, JSON-схема строгая, есть откат на mock.

**Существенный минус (наблюдался).** Guard не проверяет чипы-подсказки, а чип подставляется в поле ответа. После этого выдуманный факт становится «текстом пользователя» и проходит Guard. Примеры от живого OpenAI:
- «Завуч Иванова И.И., почта ivanova@college.ru» — вымышленный контакт;
- «За последние 2 года растёт число отчислений на 1 курсе» — вымышленный факт с числом (v_01.txt, u_05.txt).

Промпт прямо просит «2–4 вероятных варианта ответа» (internal/ai/prompts.go:31). Это противоречит главному обещанию «0 выдуманных фактов» на первом экране.
**Минимальная правка.** Прогонять `suggestions` через ту же проверку Guard: удалять чипы с числами, email, телефонами и именами собственными, которых нет в черновике и ответах. Либо в промпте разрешить только формы ответа без фактов.

### 6. Реалистичность внедрения — 5/10
**Доказательства (плюсы).** Один бинарник Go и Postgres, `docker compose up`, работа без ключа в mock-режиме. Роли развязаны: бизнес решает, команды сами выбирают задачи.

**Доказательства (минусы).**
- **Вход.** Единый демо-код 000000: зная контакт заявителя, можно войти за него (KNOWN_ISSUES).
- **Сессии в памяти.** Во время проверки мой рабочий бизнес-токен начал получать `{"error":"нужен вход бизнеса"}`. Вход слетает при перезапуске сервера (KNOWN_ISSUES).
- **Рейтинг на ключевых словах ошибается прямо на экране:**
  - критерий «На данных 2 лет найдены 3 причины, объясняющие не менее 60% отчислений» получил полный балл с причиной «есть число с единицей (2 лет)», то есть засчитан период данных, а не метрика;
  - «Кураторы групп и завуч колледжа» → «Заполнено частично: не указана роль пользователей» (5/10);
  - результат, который предложил сам AI, получил 7/15 и 8/15 «нет конкретного артефакта». Собственная подсказка системы не проходит собственную шкалу, бизнес получает противоречивый сигнал.
- Нет пилота, партнёра, данных AI Sana и описания, кто модерирует каталог. Раздел «Дальнейшее развитие» — только намерения.

**Минимальная правка.**
1. В `success_criteria` засчитывать число только рядом с метрикой (%, раз, минут, из N), а не любое «число + слово».
2. Добавить «куратор», «завуч», «студент», «преподаватель» в словарь ролей для поля «Пользователи».
3. Для вариантов результата брать артефакт из словаря рейтинга.

Для роста выше 7 нужно внешнее доказательство (письмо или интерес AI Sana, реальный черновик от бизнеса). В репо его нет, способ улучшения в рамках кода пока не найден.

## Блок B. Официальная рубрика техотбора TASK.md §9 (продуктовая сторона)

| Критерий | Макс | Балл |
|---|---|---|
| Сквозной сценарий | 20 | 18 |
| Качество карточки | 15 | 11 |
| Геймификация бизнеса | 25 | 21 |
| Каталог и отклики | 15 | 14 |
| AI функция | 10 | 6 |
| Техническое качество | 10 | 8 |
| Демонстрация | 5 | 3 |
| **Итого** | **100** | **81** |

### Сквозной сценарий — 18/20
**Доказательства.** Черновик → публикация → отклик → решение проходят и через API (#10), и в UI (#16). Ошибки обрабатываются: пустые plan, deadline, link дают 400 «обязательные поля не заполнены»; отклик без входа — «нужен вход команды»; select командой — отказ; `/api/tasks/999` → 404.
**До 20.**
- Починить «НЕ ОПУБЛИКОВАНА» в «Моих задачах» (MyTasks.tsx:41).
- Разрешать `confirm-stage` только после `accepted`.

### Качество карточки — 11/15
**Доказательства (плюсы).** Карточка из 10 полей, все поля редактируются. Пустые поля помечены «Не указано — AI не добавил фактов». В пакетном режиме 4 вопроса по делу, каждый с примером.

**Доказательства (минусы).**
- В пакетном режиме модель потеряла сведения пользователя: на ответ «Завуч Айгуль» поле `contact` осталось пустым уже в `raw_fields` модели, до Guard (`removed_by_guard: []`). Пользователь сообщил факт, а карточка его не содержит.
- Пошаговый агент в двух прогонах из трёх дошёл до лимита в 5 вопросов, не спросив о критериях успеха. README обещает, что пока не выяснены данные, критерии и контакт, агент не остановится.
- Чипы-подсказки слабые или выдуманные (см. п. 5).

**До 15.**
- Лимит в 5 вопросов не должен срабатывать раньше, чем закрыты data, success_criteria и contact. Либо честно исправить README.
- Сохранять в `contact` имя, если нет email.
- Защитить чипы через Guard.

### Геймификация бизнеса — 21/25
**Доказательства (плюсы).**
- Разбивка по 7 показателям с причиной у каждого.
- Список «Что улучшить дальше» с «+N», отсортирован по выгоде.
- Рейтинг пересчитывается при каждом сохранении: 37 → 52 → 87.
- Предварительный балл до подтверждения, официальный после.
- Позиция в каталоге видна и меняется: «место #2 из 6», «№1 в каталоге».
- Показано, сколько не хватает до следующего уровня; черновики видны с пометкой «требует уточнения».

**До 25.**
- Ложные причины подрывают главный принцип прозрачности: «есть число с единицей (2 лет)», «не указана роль» у кураторов, половина балла собственному варианту AI (см. п. 6).
- Правка опубликованной карточки снимает её с каталога (KNOWN_ISSUES). Для механики «дописал — поднялся» бизнесу это выглядит как наказание за улучшение.
- Правки: те же, что в п. 6, плюс хранить подтверждённую версию в каталоге, пока новая не подтверждена.

### Каталог и отклики — 14/15
**Доказательства.** В каталоге все подтверждённые задачи, сортировка по баллу, фильтры по направлению и уровню, поиск. Отклик на любую задачу, включая черновик. Ручные «Выбрать / Отложить / Отклонить / Обсудить». Можно выбрать 0, 1 или несколько команд: у #10 один отклик выбран, второй отклонён.
**До 15.** Рекомендации командам не персональные: для «Bilim UX» выдаются все задачи по рейтингу. Нужно ранжировать по пересечению с интересами и навыками и показывать, какие именно слова совпали.

### AI функция — 6/10
**Доказательства (плюсы).**
- Структурированные вызовы OpenAI со `strict: true`.
- Промпты открыты (`/api/ai`), виден `last_call` с сырым ответом.
- Guard для карточки, откат на mock, варианты первого результата со сроком и пометкой «оценка AI».
- Вопросы по черновику уместные.

**Доказательства (минусы, наблюдались).**
- Выдуманные контакт и факт с числом в подсказках, не покрыты Guard. Это противоречит §5 «ИИ не должен добавлять факты».
- Потерян контакт, который сообщил пользователь.
- Сроки 2, 4, 6, 8 недель в вариантах придуманы моделью. Помечены, но это всё равно не данные пользователя.

**До 10.** Guard на `suggestions` (см. п. 5) и сохранение `contact`.

### Техническое качество (продуктовый взгляд) — 8/10
**Доказательства.** README содержит всё по п. 5.4.15: архитектура, формула, правила каталога, тестовые сценарии, env, лицензии. Валидация 400/401/403/404, строгая схема, `go test`.
**До 10.**
- Расхождения README и UI: 3–4 или 3–5 вопросов; обещанная остановка агента, которой нет на практике.
- Сессии в памяти.
- Чистый запуск по README я не проверял: это задача Technical Judge.

### Демонстрация — 3/5
**Доказательства.** demo/script.md подробный (31 КБ), но сам себя называет «draft»: «Засечённого полного показа с озвучкой пока нет». Ролика в репо нет.
- Сценарий построен на mock: «На экране честно виден режим «локальные вопросы»». Для трека с обязательным OpenAI API это ослабляет показ AI-функции.
- Цифры на лендинге («30 → 90», «#5 → #1») не совпадают с цифрами сценария («55 → 100», «#4 → #1»). Жюри увидит оба экрана.

**До 5.**
- Один засечённый прогон §11 на живом OpenAI (≤5 мин) с записью.
- Одна пара цифр «до → после» и в hero, и в сценарии.

## Итог для перегруженного жюри
Сильная и честная реализация ТЗ: весь сквозной путь работает по-настоящему, рейтинг прозрачен и влияет на место в каталоге. Проект будет стоять в верхней части среди однотипных работ, но без запаса.

Жюри в первую очередь заметит три вещи:
1. AI-подсказки подставляют выдуманные контакты и цифры, хотя главный лозунг первого экрана — «0 выдуманных фактов».
2. Причины начисления баллов иногда ложные («2 лет» засчитано как метрика).
3. Экран «Мои задачи» показывает опубликованную задачу как «не опубликована».

Все три исправляются точечно, за 30–60 минут.

## Technical Judge (Opus)
# Technical Judge: Archibalt (HEAD 66c4fa2), 23.09.2026, 15:45–15:53 (+05)

## Шаг 1. Реальный запуск по README

**Клон:** `git clone https://github.com/BAITC-Hacks/hack-fee6d244-archibalt.git /tmp/jury3-clone`. Сеть была доступна, клонировал с GitHub. HEAD 66c4fa2, 171 коммит. `.env` нет, в окружении shell `OPENAI_API_KEY` пуст.

**Что менял:** только публикуемые порты, потому что 8080 и 5432 заняты dev-стендом. Использовал override `/tmp/jury3-override.yml`:
```yaml
services:
  db:  { ports: !override [] }                        # 5432 не публикуется
  app: { ports: !override ["127.0.0.1:18093:8080"] }
```
Команда: `docker compose -p jury3 -f docker-compose.yml -f override up --build -d`. Других изменений не было.

**Результат:** стек поднялся с первой попытки за 15 с (слои частично из кэша). В логе: `seed загружен teams=5 tasks=5 proposals=5`, `listening ai_mode=mock`. `GET /api/health` → `{"ai_mode":"mock","db":true,"ok":true}`, ответ совпадает с README. Фронт `/` и SPA-маршрут `/task/6` отдают 200, скриншоты сделаны через Playwright.

### Сценарий README через API (curl, mock)
| Шаг | Фактический ответ |
|---|---|
| Каталог | 5 задач, уровни priority 90 / ready 70 / working 60, 42 / draft 30. Сортировка по баллу, `?level=draft` → [1]. Пульс: 5/5/5 |
| Создать без входа | 401 «чтобы создать задачу, войдите…» |
| Вход бизнеса | код 111111 → 401, код 000000 → token |
| Пустой черновик | 400 `draft_text: опишите задачу хотя бы одной фразой` |
| Черновик «Нужно приложение для учёта заявок…» | 201, status clarifying, 5 вопросов (context, data, success_criteria, constraints, users), score 10 |
| Ответы (среди них «не знаю») | карточка 40 «рабочая». «Не знаю» распознано как заглушка (0). Пустые поля остались пустыми, AI ничего не добавил |
| Правки: критерий 30%, email, ритм | 40 → 65, затем с результатом, ограничениями и сроком передачи данных → 100 «приоритетная» |
| Чужой бизнес правит задачу / правка seed-задачи | 403 / 403 «у задачи нет заявителя» |
| До confirm | задачи нет в каталоге. После confirm: rank 1 из 6, каталог [6,4,3,2,5,1] |
| Правка опубликованной («todo…») | 85, confirmed=false, задача снята с каталога. Вернул правку и подтвердил → 100 |
| Отклик без входа / без ссылки / `javascript:` | 401 / 400 `link` / 400 «нужна ссылка вида https://» |
| Два отклика от двух команд | 201, 201 |
| Команда пытается сделать select | 401 |
| select / reject / confirm-stage | selected / rejected / stage_confirmed. Команда получила points 10, confirm-stage на rejected → 400 |
| `/api/tasks/999`, `/api/tasks/abc` | 404 / 400 |

### Тот же сценарий в браузере (Playwright, Chromium 1280×800)
Шёл строго по кнопкам UI: «Уточнить с агентом» → модалка входа → «Подставить 000000» → диалог агента: 4 вопроса (data, success_criteria, constraints, contact) → «варианты первого результата», выбрал «Выбрать» → «Собрать карточку». Затем `/task/11/edit` (77 «готовая») → правка названия, пользователей и формата → «Сохранить и пересчитать» 77 → 92 (+15) → «Подтвердить и опубликовать» → каталог «№1 в каталоге». Дальше «Я команда», вход, отклик (201), «Я бизнес», «Выбрать» (200) и статус «Выбрана, ждём ответа команды». Ошибок в консоли нет. Сетевой лог: request-code, verify, POST tasks, 4 × next-question, result-options, apply-result, answers, apply-result, PUT fields ×2, confirm, proposals, select. Все 2xx.

**Дефекты, найденные при прогоне:**
1. **«Мои задачи» показывают опубликованную задачу как «НЕ ОПУБЛИКОВАНА».** `GET /api/business/me` возвращает `rank:0, catalog_size:0` у задачи с `confirmed:true`, а `web/src/pages/MyTasks.tsx:41` выводит «не опубликована», если rank пустой. Дефект виден на шаге 8 README. Скриншот: `ui_12_mytasks.png`.
2. **Выбранный вариант результата затирает ответ пользователя.** Порядок вызовов: answers → apply-result. На вопрос о критериях я ответил «Ни одна заявка не теряется, время обработки меньше на 30%». После выбора варианта в карточке оказался шаблон mock: «Вы открываете обзор и на реальном примере… находите нужные сведения». Измеримый критерий пользователя потерян, а шаблонный текст получил 15/15.
3. **Mock-карточка низкого качества.** Название — обрезанный черновик «Нужно приложение для учёта заявок, сейчас». Контекст и потребность — одна и та же фраза, и за это начислено 20/20. Ответ «email …, созвон раз в неделю» целиком ушёл в `contact`, а `interaction_format` остался пустым. Пользователей агент не спросил.
4. **Правило остановки в mock противоречит карточке.** В пошаговом mock агент решил «критичные сведения есть… критерии успеха» по числу «20%» в ответе на другой вопрос, но в карточке `success_criteria` остался пустым (задача 7, score 32).
5. **Ширина таблицы откликов.** В «Мои задачи» кнопка «Выбрать» вылезает за край карточки при 1280 px (косметика).

**Уборка:** выполнил `docker compose -p jury3 … down -v` (volume `jury3_pgdata` удалён), `rm -rf /tmp/jury3-clone`, удалил override. Dev-стенд :8080 только читался и остался жив (`ai_mode: openai`).

## Шаг 2. Тесты, сборка, код

- `go vet ./...` — чисто. `go test ./...`: ok ai / http / rating / store, 70 PASS. **Но** `TestMigrateSeedAndQueries`, `TestSeedFKValidation`, `TestBenchModels` пропускаются (SKIP) без `TEST_DATABASE_URL`, то есть по `make test` хранилище реально не проверяется.
- `cd web && npm ci && npm run build` — `tsc -b && vite build` без ошибок, 82 модуля, JS 294 КБ (gzip 92 КБ).
- **internal/ai:** Responses API через net/http без SDK. Используется `text.format = json_schema, strict: true` (`openai.go:84-89`), таймаут 10 с (`openai.go:37`). `run()` делает 2 попытки, затем mock (`ai.go:115-131`), `last_error` доступен в `/api/ai`. Для вопросов быстрая модель, для карточки основная. Guard (`guard.go:26`) проверяет: ≥60 % значимых слов по стему, пары «число+слово», новые имена собственные, смену полярности. Применяется и к OpenAI, и к mock (`finishCard`). Правила 3..5 вопросов навязаны кодом (`mock.go:189 finishNext`). **Правило «не останавливаться, пока не выяснены данные, критерии, контакт» в OpenAI-пути кодом не проверяется:** `if r.Done && n >= Min { return }` (`mock.go:230`). Оно есть только в промпте (`prompts.go:29`) и в mock (`mock.go:151-177`). README пишет «Эти границы проверяет код поверх ответа модели», это верно только для 3..5.
- **Живой OpenAI:** со стороны жюри не воспроизводится, это нормально по дизайну. Есть статический `docs/ai-live-sample.json` (gpt-6-sol). На dev-стенде `/api/ai` показывает `mode: openai`, `last_error: null` и реальный `last_call` с `raw_fields` модели. Интеграция работает, но проверяющий видит только mock.
- **internal/rating:** веса 20/20/15/15/10/10/10 (`rating.go:25-33`), начисление 0 / половина (вниз) / полный (`rating.go:295-307`), уровни 40/70/90 (`rating.go:231`). **Формула в README совпадает с кодом.** `Compute(f, confirmed)` игнорирует `confirmed` (`_ = confirmed`). «Подтверждённость» реализована статусом публикации, а не формулой. Этот дизайн описан в README.
- **Накрутка рейтинга (проверил сам):** карточка из «кот собака луна пирог трактор облако», «команда», «пример», «5 раз», «только», «бот для», `a@b.co`, «чат раз в» → **67 «рабочая»**. «Бот для» получает 15/15, «команда» — 10/10. README признаёт это как известное ограничение.
- **internal/http:** `MaxBytesReader 1 MiB` (`handler.go:146,468`), токены `crypto/rand` (`auth.go:41`), проверки владельца 401/403, проверка схемы ссылки, WS с `OriginPatterns`.

## Шаг 3. Оценка 0–10

| # | Критерий | Балл | Доказательство | Минимальная правка для повышения |
|---|---|---|---|---|
| 1 | Реальное agentic-поведение | **5** | `next-question`: модель выбирает следующее поле, варианты ответа и момент остановки, код держит рамки 3..5 и запрещает повторы. Добавлен шаг «варианты первого результата». Инструментов, плана и самопроверки нет: это управляемая анкета из одного LLM-вызова на шаг. В mock это детерминированная таблица | Проверять `criticalFields` в `finishNext` для любого backend. Добавить шаг-верификатор («сверь карточку с источником → переспроси поле»), который реально меняет план вопросов |
| 2 | Необходимость и качество OpenAI API | **6** | Strict json_schema, две модели, повтор, откат, Guard, промпты на `/api/ai`, `last_call` с сырым ответом. Живой режим подтверждён на dev-стенде и в `docs/ai-live-sample.json`. Минус: жюри без ключа видит только mock. `result-options` вставляет в карточку шаблонный текст, а не слова пользователя | `apply-result` не должен перезаписывать непустое поле из ответа пользователя (или показывать diff). Приложить в репо лог 1–2 живых прогонов всех четырёх вызовов |
| 3 | Работоспособность end-to-end | **8** | Весь путь README прошёл и через API, и через UI на чистом клоне. Ошибок консоли нет | Заполнять `rank`/`catalog_size` в `GET /api/business/me` (дефект 1). Исправить затирание критерия (дефект 2) |
| 4 | Надёжность и проверка результата | **6** | 70 тестов, 400/401/403/404 проверены вживую, Guard. Store-тесты молча пропускаются. Mock копирует одну фразу в context и need, и формула даёт за это 20/20. Рейтинг накручивается ключевыми словами (67 за бессмыслицу) | В rating: context == need → половина веса. Для полного балла нужно ≥3 значимых слов, не только признак. В `make test` поднимать БД или явно падать без неё |
| 5 | Безопасность и приватность | **5** | Хорошо: owner-проверки, crypto/rand, лимит тела, маскирование owner_contact, контакты команд скрыты. Проблемы: (а) `GET /api/ai` без входа отдаёт черновик и ответы **последнего любого пользователя**, в том числе неопубликованной задачи (маскируются только email и телефоны). (б) `GET /api/tasks/{id}` отдаёт неопубликованные черновики любому по перебору id (задача 10 status clarifying). (в) имя команды по умолчанию «Команда +77011234567» публикует телефон в `/api/teams` и откликах. (г) имена команд не уникальны: создал вторую «Демо-команда Qadam Data». (д) общий OTP 000000 признан в KNOWN_ISSUES | `last_call` отдавать только владельцу или только для seed-демо. `GET /tasks/{id}` для неопубликованных — только заявителю. Имя по умолчанию «Команда #N». Уникальность имени |
| 6 | Техническая честность демо | **6** | Mock честно подписан в UI («Локальный режим…»), KNOWN_ISSUES откровенны. Но `demo/script.md` устарел: кнопок «Продолжить к вопросам» и «Выбрать команду» в коде нет, «5 локальных вопросов» (сейчас 4), сборка 4d41416. README «Что сознательно не делали: чат… мобильная адаптация» противоречит реализованному чату REST+WS. На лендинге абсолютное «0 выдуманных фактов», при этом шаблон `result-options` попадает в карточку. `API_CONTRACT.md` описывает привязку к seed-команде по имени, которая удалена (`auth.go:201`). README утверждает, что код проверяет остановку по критичным полям | Синхронизировать `demo/script.md` с текущими подписями кнопок. Убрать «чат» из «не делали». Смягчить «0 выдуманных фактов» либо прогонять варианты через Guard. Поправить фразу про проверку кодом |

## Шаг 4. Чеклист техотбора (отдельно от баллов)

**README по п. 5.4.15:**
1. Описание решения — есть (первый абзац).
2. Архитектура — есть (схема и модули, раздел «Архитектура»).
3. Технологии — есть (схема, таблица компонентов).
4. Установка — есть (`git clone`, Docker).
5. Запуск — есть (`docker compose up --build`, dev-вариант).
6. Зависимости — есть (Docker; Go 1.25+, Node 22+, PG16).
7. Параметры окружения — есть (таблица и `.env.example`).
8. Порядок проверки основного сценария — есть (9 шагов с ожидаемым результатом и проверкой ошибок).
9. Сторонние компоненты и лицензии — **есть, но неполно**: нет `github.com/coder/websocket` (ISC, прямая зависимость в `go.mod`) и базовых образов `node:22-alpine`, `golang:1.26-alpine`, `alpine:3.22`.

**Условия допуска:**
- Запуск по инструкции — да. Единственное отступление — порты, из-за конфликта на машине проверяющего.
- История коммитов — да. 171 коммит, 2 автора (abylay 99, iagadilov 71), коммиты каждый час с 13:00 по 15:43. До 13:00 только документы и research, кода нет.
- Раскрытие компонентов — частично (см. п. 9).
- Проверка без личных аккаунтов — да: mock без ключа, демо-код 000000.

**§10 TASK.md:** репозиторий — да. MVP запускается по инструкции — да. README с архитектурой, формулой рейтинга, правилами каталога и тестовыми сценариями — да, все четыре раздела есть. Демонстрация до 5 минут — есть сценарий, но он устарел, видео в репо нет.

**Итог техотбора: ПРОЙДЕН (условно).** Блокеров запуска нет. До 18:00 нужно дописать в таблицу лицензий `coder/websocket` (ISC) и базовые Docker-образы. Вне моей зоны (решает Gatekeeper): в команде 2 участника при «3–5» в ТЗ, команда признаёт это в KNOWN_ISSUES.

## Шаг 5. Официальная рубрика TASK.md §9, техническая сторона

| Критерий | Макс | Балл | Доказательство | Чего не хватает до максимума |
|---|---|---|---|---|
| Сквозной сценарий | 20 | **18** | Черновик → вопросы → карточка → рост 77→92 → публикация №1 → отклик команды → «Выбрать» и «Отклонить» → confirm-stage +10. Проверено и через UI, и через API | «Мои задачи» показывают опубликованную задачу как неопубликованную (`/api/business/me` rank=0) |
| Качество карточки | 15 | **11** | ≥3 уместных вопроса по одному полю каждый, с подсказками. 10 полей, все редактируемые, пустое остаётся пустым, «не знаю» распознаётся | В mock: название — обрезок черновика, context = need дословно, contact и формат не разделены, users не спрошены. Выбор варианта результата затирает критерий пользователя |
| Геймификация бизнеса | 25 | **21** | Прозрачная разбивка по 7 показателям с причинами, `missing` с `gain`, пересчёт на каждое сохранение, «будете #3 из 7», место в каталоге меняется, уровни по ТЗ, формула открыта | Накрутка: дубль context/need = 20/20, «бот для» = 15/15, бессмыслица = 67. Правка опубликованной задачи снимает её с каталога (признано) |
| Каталог и отклики | 15 | **14** | Опубликованные задачи видны всем, в том числе черновики 30. Сортировка score DESC. Фильтры отрасль/уровень и поиск. Число откликов не ограничено. Только ручные select/reject/hold, автоназначения нет | Телефон в имени команды по умолчанию, неуникальные имена команд |
| AI функция | 10 | **7** | Структурированный strict JSON, Guard, fallback, прозрачность `/api/ai`, живой режим подтверждён на стенде | Шаблонный текст `result-options` попадает в карточку мимо Guard и перетирает факты пользователя. Жюри без ключа видит только mock |
| Техническое качество | 10 | **8** | Запуск одной командой, валидация 400/401/403/404, чистая структура `internal/*`, 70 тестов, сборка без ошибок | Store-тесты пропускаются без БД. Документация расходится с кодом (API_CONTRACT о привязке seed-команды, README о чате) |
| Демонстрация | 5 | **3** | Сценарий живой и полностью работает в MVP (§11 выполнимо) | `demo/script.md` ссылается на несуществующие кнопки и старую сборку, видео нет |
| **Итого** | 100 | **82** | | |

Статус: **NOT READY** (82/100). Приоритет правок до код-фриза, по соотношению цены и эффекта:
1. `rank` в `/api/business/me`.
2. `apply-result` не перетирает непустые поля.
3. Лицензия `coder/websocket`.
4. Синхронизировать `demo/script.md` и README («чат», «проверяет код»).
5. Закрыть `last_call` и неопубликованные `GET /tasks/{id}`.
6. Имя команды по умолчанию без телефона.
7. context == need → половина веса.

## Red Team Judge (Astra, gpt-6-astra xhigh)
**Вердикт: текущую версию не рекомендую в финал.** Основные основания — ненадёжная оценка готовности, чрезмерные гарантии AI и разрыв сценария на стартовых данных.

Проверка исходников: `66c4fa2`. Запрещённые внутренние материалы не читал. GET `/api/health` и `/api/ai` завершились `curl: (7) Couldn't connect to server`; браузер недоступен. Поэтому ниже — доказательства из файлов и статический анализ, **не утверждение о проверенном отказе запуска**. Техотбор и Demo Day оцениваю раздельно.

**1. Основания для отклонения**

**1) Максимальные баллы выдаются за неизмеримый критерий успеха**

- **Наблюдение:** тест прямо требует начислять **15/15** за «Улучшить качество на 50%» — [qa_cases_test.go:19](/Users/abylay/alemhack/internal/rating/qa_cases_test.go:19). Реализация замечает отсутствие способа измерения, но всё равно возвращает полный балл — [rating.go:396](/Users/abylay/alemhack/internal/rating/rating.go:396).
- **Почему отклонить:** центральная механика поощряет формулировку, по которой нельзя принять работу студентов. Прозрачность арифметики не доказывает качество оценки. Это бьёт по самому дорогому критерию техотбора — **геймификация бизнеса, 25 баллов**.
- **Минимальная правка:** не давать полный балл абстрактной метрике без определения измерения; изменить соответствующий тест. Проверить, что добавление реального способа приёмки повышает оценку.

**2) Обещание «0 выдуманных фактов» технически не обеспечено**

- **Наблюдение:** гарантия вынесена на главный экран — [Landing.tsx:68](/Users/abylay/alemhack/web/src/pages/Landing.tsx:68). Guard проверяет совпадение 60% значимых слов, а проверку отрицаний явно отключает для `context` и `need` — [guard.go:25](/Users/abylay/alemhack/internal/ai/guard.go:25).
  
  Статический контрпример: исходник «Не хотим передавать клиентские данные стороннему сервису», поле `need` — «Хотим передавать клиентские данные стороннему сервису». Значимые слова совпадают; инверсия смысла проходит эти условия.
- **Почему отклонить:** нарушается содержательное требование TASK §5. Человеческое подтверждение обязательно само по себе и не превращает ошибку модели в достоверный результат. Под ударом **AI-функция, 10**, и **качество карточки, 15**.
- **Минимальная правка:** для фактических полей использовать проверяемые цитаты из источника либо сохранять исходную формулировку при сомнении. Убрать абсолютную гарантию из интерфейса. Одной замены текста недостаточно для исправления поведения.

**3) Все пять стартовых задач ведут к откликам, по которым никто не может принять решение**

- **Наблюдение:** чтение JSON подтвердило: задачи 1–5 опубликованы, у всех `owner_contact=""` — [seed/tasks.json:1](/Users/abylay/alemhack/seed/tasks.json:1). `requireOwner` возвращает 403 при отсутствии владельца; этот же барьер запрещает назначить владельца через API — [handler.go:700](/Users/abylay/alemhack/internal/http/handler.go:700). Отклик на опубликованную задачу при этом разрешён.
- **Почему отклонить:** эксперт открывает готовый каталог, отправляет предложение и попадает в тупик. На всём поставляемом каталоге недоступна обязательная половина сценария — решение бизнеса. Это касается **сквозного сценария, 20**, и **каталога и откликов, 15**.
- **Минимальная правка:** назначить seed-задачам демонстрационных заявителей и указать контакты входа в README; проверить выбор и отказ на этих данных.

**4) При ошибке API сохраняется работа интерфейса, но ухудшается содержание карточки**

- **Наблюдение:** повтор и fallback действительно реализованы — [ai.go:112](/Users/abylay/alemhack/internal/ai/ai.go:112). Однако mock считает поля раскрытыми по словам исходного описания и может их не спрашивать — [mock.go:51](/Users/abylay/alemhack/internal/ai/mock.go:51). При сборке карточки он переносит ответы в соответствующие поля, а из черновика отдельно извлекает только название, контекст и потребность — [mock.go:260](/Users/abylay/alemhack/internal/ai/mock.go:260).
  
  Например, сообщение о выгрузке из CRM может остаться только внутри общего контекста: вопрос о данных пропущен, поле `data` пустое.
- **Почему отклонить:** «не упало» не означает «сценарий выдержал ошибку API». Пользователь уже сообщил сведения, но карточка и рейтинг их не учитывают. Под ударом качество карточки и техническое качество.
- **Минимальная правка:** в fallback явно уточнять поля, которые он не умеет извлекать из черновика, либо переносить в них подтверждённые фрагменты. Добавить одну проверку сохранения сведений при отказе OpenAI на сборке карточки.

**5) Главный экран демонстрирует расчётный результат как уже полученный**

- **Наблюдение:** `useExample` вычисляет «после», складывая потенциальные `gain` до достижения 90; новой карточки и её серверного пересчёта здесь нет — [BeforeAfter.tsx:27](/Users/abylay/alemhack/web/src/components/BeforeAfter.tsx:27). Главная подписывает результат «Стало» и «место после ответов» — [Landing.tsx:83](/Users/abylay/alemhack/web/src/pages/Landing.tsx:83). В анимации каталога движение к первому месту определяется прокруткой — [ScrollStory.tsx:122](/Users/abylay/alemhack/web/src/components/ScrollStory.tsx:122).
- **Почему отклонить:** это иллюстрация предполагаемого результата, а не доказательство работы. Перегруженное жюри увидит красивое «до/после», которое нельзя засчитать как выполнение §11. Рост собственного балла также не доказывает сокращения времени подготовки задачи или повышения качества студенческой работы.
- **Минимальная правка:** обозначить блок как расчётную иллюстрацию; для доказательства показывать две реально сохранённые версии, ответы API и фактические позиции. Эффект для пользователей измерить отдельно.

**6) Неподтверждённые бизнес-описания доступны постороннему читателю**

- **Наблюдение:** `GET /api/tasks/{id}` возвращает задачу без проверки владельца и статуса публикации — [handler.go:365](/Users/abylay/alemhack/internal/http/handler.go:365); хранилище выбирает любой статус — [store.go:156](/Users/abylay/alemhack/internal/store/store.go:156). `publicTask` маскирует только `owner_contact`, сохраняя черновик, ответы и поля.
- **Почему отклонить:** ручное подтверждение не является реальной границей раскрытия информации. На настоящих бизнес-данных чужой пользователь может получить ещё не опубликованные сведения по числовому ID. Это конкретный дефект технического качества и препятствие масштабированию.
- **Минимальная правка:** отдавать неопубликованную задачу только владельцу; публичный ответ опубликованной карточки ограничить необходимыми полями. Аналогично ограничить диагностическую трассу `/api/ai`.

**7) Обязательное использование OpenAI и заявленная самостоятельность агента доказаны недостаточно**

- **Наблюдение:** README называет OpenAI опциональным — [README.md:21](/Users/abylay/alemhack/README.md:21). Описанный завершённый демопрогон использовал mock — [demo/script.md:3](/Users/abylay/alemhack/demo/script.md:3). `last_call` создаётся общей функцией и для mock; собственного признака провайдера в записи нет — [guard.go:217](/Users/abylay/alemhack/internal/ai/guard.go:217). После трёх вопросов код принимает `done=true` без независимой проверки критичных сведений — [mock.go:230](/Users/abylay/alemhack/internal/ai/mock.go:230).
- **Почему отклонить:** наличие клиента OpenAI доказывает интеграционный код, но не успешное использование обязательного API в оцениваемом сценарии. Самостоятельность ограничена последовательностью промптов и правилами остановки; полезность этой адаптации не показана.
- **Минимальная правка:** предъявить воспроизводимый проход через OpenAI с привязанными к задаче провайдером, моделью и идентификатором ответа; отдельно показать fallback. Проверять критичные сведения перед остановкой либо честно сообщать, что диалог закончился с пробелами.

**Весь продукт одной тонкой обёрткой назвать нельзя:** каталог, рейтинг и решения по откликам реализованы отдельно. Но преимущество именно AI-части над обычной анкетой не доказано. TASK §5 допускает такую AI-функцию; требовать произвольного «мультиагента» вместо официального минимума было бы неправильно.

**8) Отличие от существующих продуктов построено на слабом сравнении**

- **Наблюдение:** интерфейс сравнивает решение с анкетой, чатом LLM и Excel/WhatsApp — [Landing.tsx:115](/Users/abylay/alemhack/web/src/pages/Landing.tsx:115). На текущий 2026 год Riipen документирует создание проектов бизнеса для учащихся, включая генерацию проекта через AI; Parker Dewey предлагает просмотр проектов, подачу заявок и работу после выбора работодателем. [Riipen](https://help.riipen.com/en/articles/7336872-creating-a-project), [Parker Dewey](https://www.parkerdewey.com/career-launchers).
- **Почему отклонить:** сочетание «бизнес-задача → студенты → отклик» уже существует, AI-подготовка проекта тоже. Преимущество рейтинга готовности заявлено, но не сравнено с ближайшей альтернативой. Это основание проиграть по **инновационности Demo Day, 15 баллов**, а не самостоятельное нарушение техотбора.
- **Минимальная правка:** сравнить один одинаковый запрос с Riipen или сильной анкетой по конкретным результатам: пропущенные сведения, ручные исправления, время до пригодной карточки. Если преимущества нет — правка позиционирования его не создаст.

**2. Точная формулировка кейса и обязательная демонстрация**

| Требование | Что установлено |
|---|---|
| **TASK §10: исходники и README** | Ссылка, архитектура, формула, правила каталога и тестовые сценарии присутствуют. Основания объявлять эти результаты отсутствующими нет. |
| **TASK §10: запускаемый MVP** | Самостоятельный запуск в этой проверке не подтверждён. Недоступность моего GET недостаточна для вывода «проект не запускается». |
| **TASK §10–11: полный показ ≤5 минут** | В сценарии зафиксированы **71 секунда без речи** и отсутствие засечённого показа с озвучкой. Это не доказательство превышения лимита, но и не подтверждение его соблюдения. |
| **TASK §11: все переходы в действующем MVP** | Документированный прогон относится к другой сборке. Текущая форма открывает диалог агента — [TaskNew.tsx:23](/Users/abylay/alemhack/web/src/pages/TaskNew.tsx:23); сценарий описывает прежнюю кнопку «Продолжить к вопросам». Актуальный полный проход не доказан. На seed-задачах решение бизнеса заблокировано. |
| **TASK §5: отсутствие выдуманных сведений** | Контрпример Guard приведён выше. Ручное подтверждение не снимает это отдельное требование. |
| **Положение 5.4.15 и раскрытие компонентов** | В коде импортирован `github.com/coder/websocket` — [ws.go:14](/Users/abylay/alemhack/internal/http/ws.go:14), но в списке компонентов README его нет. Минимальная правка — добавить компонент и лицензию; умышленное сокрытие этим не доказано. |
| **Состав команды** | TASK требует 3–5 участников, README перечисляет двух. [KNOWN_ISSUES.md:15](/Users/abylay/alemhack/KNOWN_ISSUES.md:15) ссылается на противоречащее правило 1–3 и заданный вопрос, но не содержит решения организатора. Расхождение не закрыто; автоматическую дисквалификацию не утверждаю. |

**Скрытого человека за автономностью не установлено.** Подготовленные ответы и ручные дополнения прямо раскрыты в [demo/script.md:19](/Users/abylay/alemhack/demo/script.md:19). Более того, §10 разрешает заранее подготовленный пример, а §11 требует ручного решения бизнеса. Проблема — приписывать этому примеру доказанную автономность или пользовательский эффект.

**3. UNPROVEN CLAIMS — что заявлено, но не доказано**

- **«0 выдуманных фактов»** — абсолютная гарантия опровергается условиями Guard.
- **«90–100 означает готовность к работе»** — показано выполнение эвристик, не возможность начать работу без уточнений.
- **«Агент останавливается, когда сведений достаточно»** — число вопросов и решение модели не равны проверке достаточности.
- **«Ошибка API не нарушает сценарий»** — повтор реализован; сохранение полноты карточки при деградации не доказано.
- **«55 → 100 улучшило бизнес-задачу»** — рост внутреннего показателя описан; независимая оценка полезности отсутствует.
- **«Решение эффективнее анкеты или аналогов»** — нет сравнительного измерения времени, качества или объёма ручной работы.
- **«Текущий полный сценарий укладывается в пять минут»** — актуального засечённого показа с озвучкой в разрешённых материалах нет.
