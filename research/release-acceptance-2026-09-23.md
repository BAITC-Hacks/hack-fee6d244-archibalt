# Выпускная приёмка Education MVP — 23.09.2026

Основа: официальные `TASK.md` §§3–5, 9–11; проверочный маршрут — `research/acceptance-matrix.md`. Состояние сверено с `STATUS.md` и `research/jury-review-3.md` на `origin/main` **`51a7685`**. Это чеклист доказательств, а не новый балл жюри. Оценка жюри №3 **81/100** относится к `66c4fa2`; её нельзя переносить на сборку после merge потоков A/B/C. Локальный mock AI допустим по §5, если показаны промпт, формат и обработка ошибки. Регистрация, чат и мобильная вёрстка не обязательны по §7.

Обозначения: **доказано ранее** — указан конкретный коммит и способ; **повторить** — результат на финальном SHA ещё не получен. Владелец результата: Ильяс координирует выпуск и демо; Абылай отвечает за Go/AI/рейтинг; frontend-владелец — за `web/**`; независимый QA — за повторный чистый прогон. Новые владельцы и изменения статуса фиксируются только в `STATUS.md` координатором.

| Гейт / вес §9 | Последнее проверяемое доказательство | Что изменилось и что повторить после merge A/B/C | Что увидит судья; владелец |
| --- | --- | --- | --- |
| **Техдопуск и чистый запуск** / 10 | Жюри №3 подняло чистый клон `66c4fa2` по README за 15 с, health/seed и §11 прошли; документация исправлялась в `9b19f93`, `58dfd4d`. | К `51a7685` добавлены чат, seed с заявителями (`3de8c68`), новые зависимости, `dev.sh`, каталог команд и прямой вход в чат с лендинга. **Повторить** именно `README.md` → чистый clone/новый Postgres volume → `docker compose up --build`, `go test ./...`, `/api/health`, 5 задач/5 команд/5 откликов, ошибки 400/401/403/404. Не использовать общий `:8080` или прежний volume. | Судье: ссылка на репозиторий, README с запуском/архитектурой/формулой/тестовыми сценариями, живой URL и health. Владелец: Абылай + Ильяс; независимое свидетельство: QA. |
| **Холодный первый экран** / вход в показ | Жюри №3 дало PASS по экрану `66c4fa2`: запрос → 0–100 и «было/стало» считываются за 3–5 с; прежний независимый screenshot gate был на `9de7833`. | Лендинг и вход в чат менялись вплоть до `0ea237c`. **Повторить** реальный screenshot 1280×720 и 10 секунд без пояснений: кто бизнес/команда, первый клик, результат; проверить CTA в первом viewport и куда он ведёт. Сверить микротекст с фактическим UI и правилами (список ниже). | Судье: первый экран приложения и первый клик в работающий агент/каталог. Владелец: frontend + Ильяс; независимый взгляд: QA. |
| **Реальный путь §11** / 20 | Жюри №3 провело API и браузерный путь на `66c4fa2`; независимый QA раньше прошёл raw→5 вопросов→80→100→каталог #1→вход команды→отклик→решение на `67903e5`. | Новый `AgentChat`, варианты результата, owner guards и прямой чат с лендинга (`0ea237c`) сменили экранный маршрут. `demo/script.md` в `STATUS.md` отмечен устаревшим для чата. **Повторить на одном финальном SHA**: слабый сырой текст → ≥3 уместных вопроса → редактируемая карточка → ручное подтверждение → публикация → команда сама откликается → бизнес выбирает/отклоняет. Ни один переход не заменять слайдом. | Судье: непрерывная работа в UI и финальные статусы откликов; API — лишь страховочное доказательство. Владелец: frontend + Абылай; сценарий и приёмка: Ильяс/QA. |
| **AI и контроль человека** / 10 + карточка 15 | На `66c4fa2` есть живой OpenAI-сэмпл `docs/ai-live-sample.json`; mock/Guard/strict JSON подтверждены кодом и тестами. Жюри №3 нашло потерю контакта и подсказки мимо Guard; последние правки `58dfd4d`, `9e6bee1`, `b8dc9f1` не прошли новый независимый UI-прогон. | **Повторить** для слабого запроса и фраз «не знаю»/приветствия: 3–5 вопросов, поля без выдуманных фактов, сохранение ответа заявителя, ручная правка всех 10 полей и подтверждение до публикации. В mock показать промпт, вход/выход и ответ на ошибку; live AI показывать лишь при реально проверенном ключе/режиме. | Судье: диалог, редактируемая карточка, `/api/ai` или README с промптом/схемой/ошибкой; `docs/ai-live-sample.json` — пример, не доказательство live на его стенде. Владелец: Абылай (AI) + frontend (экран); QA проверяет. |
| **Рейтинг до/после и низкие задачи** / 25 | На `67903e5` QA видел preview **80→100** и #1; на `66c4fa2` жюри видело 37→52→87. Семь весов §4, breakdown, `+N` и четыре уровня есть. Жюри №3 зафиксировало кейс C (абстрактные «50%» дают 15/15) и бессмыслицу; часть правил менялась в `58dfd4d`, `9e6bee1`, работа B/C ещё не слита. | **Повторить после B/C** на чистой БД: начальный и подтверждённый балл, ручное дополнение и повторное подтверждение, позиция до/после; проверить 0–39 видна и допускает отклик, 40–69/70–89/90–100 корректно помечены. Прогнать кейсы `research/rating-qa-cases.md` A–E; C объявлять ограничением, если не исправлен. Не выдавать иллюстрацию лендинга за сохранённую историю. | Судье: два реальных состояния одной задачи, breakdown с суммой 100 и `+N`, каталог с местами/низким рейтингом. Владелец: Абылай (формула/seed), frontend (объяснение), Ильяс (числа показа), QA. |
| **Открытые отклики и выбор 0/1/нескольких** / 15 | На `66c4fa2` жюри проверило сценарии через API/UI; QA `67903e5` проверил авторизованный отклик, select, stage +10 и reload; reject проверен ранее. Seed получил `owner_contact` в `3de8c68`, значит прежний анонимный сценарий для стартовых задач больше не применим. | **Повторить** на финальном SHA: 0 откликов → пустое состояние; 1 → идея/план/ссылка и ручной выбор; ≥2 → сравнение, выбор одной/нескольких или отказ от всех. Бизнес входит кодом владельца, если задача защищена; команда — своим кодом. Проверить отсутствие автоподбора и лимита откликов, reload статусов; этап +10 отдельно и без двойного начисления. | Судье: публичный каталог, список предложений с 0/1/несколькими, действие бизнеса и подтверждённый статус. Владелец: Абылай (права/seed), frontend (формы), Ильяс/QA (демо). |
| **Демонстрация** / 5 | `demo/script.md` фиксирует **71 с кликов без речи** на `4d41416`; жюри №3 на `66c4fa2` не видело засечённого устного показа, видео отсутствовало. | **Повторить после A/B/C** на выбранном чистом стенде: T0–T6 одним таймером с произнесённым текстом, все UI-переходы и фактические баллы/места, **≤5:00**. Затем записать наш короткий ролик **≤2:00**; это внутренний формат, не дополнительное требование ТЗ. После репетиции восстановить чистую БД для защиты. | Судье: живой показ или непрерывный ролик с видимыми переходами; в протоколе SHA, AI mode, время, баллы/места и ошибки. Владелец: Ильяс (показ), демо-исполнитель (запись), QA (сверка). |

**Микротекст для проверки по финальному screenshot и поведению, пока не FAIL.** В исходниках `51a7685` встречаются «3–4» в сравнении на лендинге при «3–5» в остальных экранах (`Landing.tsx`, `TaskNew.tsx`); §2 требует ≥3 **уместных** вопросов. `ScrollStory.tsx` обещает «до первого места», хотя место зависит от рейтинга и других задач. Лендинг говорит «ничего не додумывая», а Guard имеет известные семантические пределы; «за три минуты» ещё не измерено с речью. «Команды выбирают сами» может смешать выбор задачи командой с ручным выбором исполнителя бизнесом. FAQ «регистрация не нужна» следует сопоставить с обязательным входом по коду для действий. Frontend-владелец оценивает точечные формулировки только после сверки живого первого экрана; QA не меняет `web/**`.

## Точный маршрут выпуска после merge A/B/C

1. Координатор фиксирует **один финальный SHA** и список вошедших A/B/C-коммитов в `STATUS.md`; владельцы сверяют README, seed, код и текущий `demo/script.md`. До этого не ставить новый PASS и не экстраполировать 81/100.
2. QA берёт этот SHA в **отдельном чистом clone/DB**, запускает строго по README, записывает health/AI mode, тесты и численность seed. На том же коде снимает 1280×720 первый экран и проверяет первый клик.
3. На новой задаче QA проходит §11 в браузере с фактическими баллами/местами; отдельно проверяет рейтинг A–E и низкую задачу, 0/1/≥2 отклика, права обеих ролей, select/reject и повтор после reload. Любой FAIL привязывается к SHA, шагу и HTTP/UI сообщению и возвращается владельцу без фиктивного PASS.
4. Ильяс обновляет устный текст под фактический чат и числа. На выбранном стенде команда засекает **≤5:00 с речью**, затем экспортирует и проверяет короткий **≤2:00** ролик. QA сверяет, что ролик показывает работающий UI, а не склейку, скрывающую сломанный переход.
5. Только после результатов 2–4 координатор обновляет `STATUS.md`, README/ограничения и итоговый jury gate. Раздельно фиксировать обязательные гейты ТЗ и улучшения качества; отсутствие live OpenAI не является отказом, если mock отвечает §5. Вопрос состава команды закрыт владельцем проекта и в выпускной QA-гейт не входит.

Три ближайших риска: **новый чатовый маршрут §11 после merge ещё не принят независимо; рейтинг B/C и баллы/позиции показа не сверены; устный ≤5:00 и короткий ролик не измерены на финальном SHA**.
