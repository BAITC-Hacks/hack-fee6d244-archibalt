# Статус команды Archibalt

Обновлено: 23.09.2026, 13:37 (Астана). Это единственный актуальный статус команды. Перед началом задачи подтяни `origin/main`, прочитай этот файл и `AGENT_HANDOFF.md`; ответственный обновляет статус после завершения задачи или изменения блокера.

## Что строим

Официальный кейс Education — **AI Sana Challenge Hub: от бизнес-задачи к решению**. Полное ТЗ и рубрика опубликованы в `TASK.md` (`ec2690d`). Утверждён продуктовый сценарий A: сырой запрос бизнеса → минимум 3 AI-вопроса → редактируемая и подтверждённая карточка → прозрачный рейтинг 0–100 → открытый каталог → свободный отклик команды → ручной выбор бизнеса. Окончательный стек: Go JSON API + PostgreSQL + Vite/React/TypeScript. `PLAN.md` задаёт объём MVP, `API_CONTRACT.md` — интерфейс и формат seed. Технический сквозной сценарий пройден; дальше приёмка демо, seed и UX.

## Кто за что отвечает сейчас

| Участник и агент | Текущая задача | Следующий результат |
| --- | --- | --- |
| Ильяс / Codex | Координация и приёмка; frontend-исполнитель владеет `web/**` (React-поток `7954407`, e2e PASS по его отчёту), seed-исполнитель — `seed/*.json`; черновик `demo/script.md` опубликован | Сверить seed-баллы и сценарий демо, провести репетицию на машине Ильяса, довести UX |
| Абылай / Claude | Go API запущен и запушен (`29411c9`): эндпоинты API_CONTRACT, Postgres, seed-загрузчик, Docker, тесты и e2e curl. README готов (`f4971af`); формула против заглушек и регрессия на seed опубликованы (`92e966a`) | Проверить тесты и Docker после изменения формулы, затем `/jury-review code` |
| Сессия 1 / Codex | Ресерч организаторов опубликован в `research/organizers.md` (`c584d4e`) | После публикации ТЗ проверить прежние гипотезы на соответствие заданию |
| Сессия 2 / Codex | Ресерч конкурентов и победителей опубликован в `research/landscape.md` (`aa12994`) | После публикации ТЗ проверить релевантность найденных решений и ниш |

Новые задачи и владельцы фиксируются здесь перед параллельной работой. Один файл или модуль одновременно меняет один ответственный.

## Что уже работает

- Общий Git-репозиторий `BAITC-Hacks/hack-fee6d244-archibalt`, основная ветка `main`.
- `HACKATHON_RULES.md` и `.claude/skills/jury-review/SKILL.md` опубликованы.
- `HACKATHON_STRATEGY.md` и `JUDGE_AGENT_PROMPT.md` опубликованы.
- Полное официальное Education-ТЗ и рубрика доступны в `TASK.md`.
- Варианты продукта опубликованы в `research/product-options.md` (`0f5e90d`); принято направление A. Актуальный технический план — `PLAN.md`, контракт API и seed — `API_CONTRACT.md` (`4ba288b`). Матрица приёмки — `research/acceptance-matrix.md`.
- Seed-данные опубликованы в `seed/*.json` (`16ec791`); модель данных и модуль рейтинга с тестами — в `internal/model` и `internal/rating` (`c15a03a`); AI-модуль с mock-режимом и тестами — в `internal/ai` (`6395277`). Go API, Postgres-хранилище, seed-загрузчик и Docker опубликованы (`29411c9`); по отчётам Абылая и QA `go test`, Docker/Postgres и API e2e прошли, включая отклик на задачу с 30 баллами и пересчёт до 90 после подтверждения.
- React-поток опубликован (`7954407`). По отчёту frontend-исполнителя браузерный e2e с живым Go API прошёл: сырой текст → 5 mock-вопросов → правка карточки (preview 75→95) → подтверждение и 1-е место в каталоге → отклики → ручной выбор и отказ. Фильтр `industry=Культура&level=draft`, понятная 404 для несуществующей карточки и начисление 10 баллов команде за подтверждённый этап также проверены. `npm run build` и Docker build прошли.
- Продуктовый ресерч `research/icp-ux-decision-tree.md` завершён (`e12d0ee`, `bc2e70f`). Для демо предложен представитель МСП с конкретной операционной задачей и примером данных; это рабочая гипотеза, спрос не подтверждён.
- SSH работает в обе стороны по ключам. Вход к Mac Ильяса разрешён только ключу Абылая с `10.19.24.78`; принудительная команда допускает чтение проекта, сообщения, выбранные Git-команды и `codex exec`. Абылай проверил ограничения и запуск `codex --version`.
- Сообщения Claude идут через `.agents/outbox.md` на Mac Абылая или `.agents/inbox.md` в этом проекте; задача Codex проверяет новые строки раз в минуту.
- Черновик `demo/script.md` опубликован (`5fc7d04`): сценарии на 2 и до 5 минут, FAQ; цифры и позиции нужно сверить после репетиции.
- README с запуском, архитектурой и тестовыми сценариями опубликован (`f4971af`). По отчёту Абылая `docker compose up --build` с нуля прошёл: SPA, health и 5 seed-задач.

## Доступы и кредиты (без секретов)

Ваучеры активированы: OpenAI Platform показывает $50 Applied promotions; NVIDIA Brev показывает $50 Current Balance в разделе Pay For Compute; Ильяс активировал промомесяц ChatGPT & Codex Pro. Кредит Brev подтверждён для compute, но его применимость к NVIDIA API Catalog не подтверждена. Исследование вариантов — `research/credits-options.md`. Промокоды, API-ключи и скриншоты с открытыми кодами в Git не публиковать.

## Что блокирует следующий шаг

1. Технический e2e пройден по отчётам исполнителей. Нужны повторный прогон и репетиция защиты на машине Ильяса, сверка текста демо с фактическим интерфейсом и баллами.
2. Формула и регрессионный тест на реальные seed-задачи опубликованы (`92e966a`): ожидаемые баллы по задачам 1–5 — 30/60/80/90/42, все четыре уровня представлены. Повторить API-прогон на этой версии и обновить цифры в `demo/script.md`; на машине Ильяса Go runtime отсутствует.
3. Предварительная независимая оценка жюри на более раннем `94be669` — 83/100, допуск к подаче не дан. После `92e966a` нужен повторный review текущей версии и засечённый показ до 5 минут; live OpenAI в этой оценке не проверялся.
4. Запущенный API на `127.0.0.1:8080` пока использует старую непустую Postgres volume: проверка каталога в 13:37 дала баллы задач 1–5: 30/50/70/85/42 и дополнительную задачу 8. `SeedIfEmpty` не обновляет такую БД. Для достоверного демо нужен отдельный свежий volume либо безопасный пересчёт без удаления общей volume, затем проверка фактических баллов через API.

## Обмен важными обновлениями

Полный протокол — `AGENT_HANDOFF.md`. После выбора идеи, завершения задачи, изменения владельца, появления блокера или важного коммита ответственный обновляет `STATUS.md` (либо передаёт координатору итог для него), коммитит и пушит результат, затем уведомляет другого агента короткой строкой с сутью и хешем коммита. Устный или SSH-апдейт не заменяет запись в Git.

- Ильяс → Claude: сообщение в `.agents/inbox.md` на Mac Абылая через существующий исходящий SSH-канал.
- Claude → Ильяс: сообщение в `.agents/outbox.md` на Mac Абылая либо в локальном `.agents/inbox.md` через ограниченный входящий SSH; Codex проверяет оба файла.
- Перед изменением общих файлов следовать правилам раздела «Работа в дуо» в `HACKATHON_RULES.md`; не переписывать чужую историю.

Следующий общий контрольный пункт: изолированный запуск с актуальным seed и формулой, сверка баллов через API, засечённая репетиция до 5 минут и повторный jury review текущего коммита.
