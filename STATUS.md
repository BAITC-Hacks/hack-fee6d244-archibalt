# Статус команды Archibalt

Обновлено: 23.09.2026, 15:42 (Астана). Это единственный актуальный статус команды. Перед началом задачи подтяни `origin/main`, прочитай этот файл и `AGENT_HANDOFF.md`; ответственный обновляет статус после завершения задачи или изменения блокера.

## Что строим

Официальный кейс Education — **AI Sana Challenge Hub: от бизнес-задачи к решению**. Полное ТЗ и рубрика опубликованы в `TASK.md` (`ec2690d`). Утверждён продуктовый сценарий A: сырой запрос бизнеса → минимум 3 AI-вопроса → редактируемая и подтверждённая карточка → прозрачный рейтинг 0–100 → открытый каталог → свободный отклик команды → ручной выбор бизнеса. Окончательный стек: Go JSON API + PostgreSQL + Vite/React/TypeScript. `PLAN.md` задаёт объём MVP, `API_CONTRACT.md` — интерфейс и формат seed. Технический сквозной сценарий пройден; дальше приёмка демо, seed и UX.

## Кто за что отвечает сейчас

| Участник и агент | Текущая задача | Следующий результат |
| --- | --- | --- |
| Страница задачи / Codex | По запросу Абылая обновлён `/task/:id` для исполнителя: сначала работа, результат и приёмка; затем данные, условия и связь. Заметное «Хочу выполнить задачу», просторная форма, отклики карточками, рейтинг раскрывается отдельно. Изменения изолированы в `TaskShow.tsx` и `TaskShow.css` | Сборка PASS; Safari: первый экран, переход к отклику, валидация пустой формы и ширина 390 px проверены. Роли и API сохранены, замечания независимого ревью исправлены |
| Каталог / Codex | По запросу Абылая полностью обновлён `/catalog`: поиск с URL, фильтры и сброс, три сортировки, карточки с сутью и результатом, адаптивная сетка и сворачиваемые фильтры. Изменения изолированы в `Catalog.tsx`, `Catalog.css`, `catalog-model.ts` | Сборка и `node --experimental-strip-types web/catalog-check.mjs` PASS; загрузка живого каталога проверена в Safari; независимое ревью кода без блокеров |
| Ильяс / Codex | Координация и приёмка; полный основной браузерный путь после `17456c5` прошёл независимый QA на чистой БД с обязательным входом и подтверждением этапа; сценарий T0–T6 уточнён в `b8a0093` | Провести репетицию с речью на чистом стенде `:18089` в 17:00; отдельно решить, входит ли `owner_contact`/вход бизнеса в показ |
| Абылай / Claude | Бэкенд: обязательный заявитель, динамические вопросы, чат WS, места/прогноз/статистика; фронт: лендинг, UI-kit, вход, каталог, редактор, отклики, /ai. Диалог с агентом (`AgentChat`) с шагом «варианты первого результата» подключён к живому API в `9c9261c`, контракт — `a0624f2`; путь по API прошёл на стенде `:8080` (5 вопросов → 3 варианта за ~3.6 с → выбор → сборка → повторное применение, поля и рейтинг на месте). Браузерный e2e диалога прошёл в headless Chrome на production-сборке против живого API: создание → вопросы по одному → варианты → выбор → «Собрать карточку» → `/task/:id/edit`; Esc/повтор/401→вход проверены, мобильная ширина починена в `68d31ca`. | Полное жюри №3, репетиция в 17:00; независимый браузерный прогон диалога на стенде Ильяса |
| Сессия 1 / Codex | Ресерч организаторов опубликован в `research/organizers.md` (`c584d4e`) | После публикации ТЗ проверить прежние гипотезы на соответствие заданию |
| Сессия 2 / Codex | Ресерч конкурентов и победителей опубликован в `research/landscape.md` (`aa12994`) | После публикации ТЗ проверить релевантность найденных решений и ниш |
| Визуальный ресерч / Codex | Опубликовал `research/analogues-visual-ux.md` (`76d5433`) со скриншотами и выводами по Riipen, Typeform, Catchafire, Parker Dewey и Jective; `web/**` не менял | Frontend-владелец применяет только проверенные точечные идеи, без общего редизайна |

Новые задачи и владельцы фиксируются здесь перед параллельной работой. Один файл или модуль одновременно меняет один ответственный.

## Что уже работает

- Общий Git-репозиторий `BAITC-Hacks/hack-fee6d244-archibalt`, основная ветка `main`.
- `HACKATHON_RULES.md` и `.claude/skills/jury-review/SKILL.md` опубликованы.
- `HACKATHON_STRATEGY.md` и `JUDGE_AGENT_PROMPT.md` опубликованы.
- Полное официальное Education-ТЗ и рубрика доступны в `TASK.md`.
- Варианты продукта опубликованы в `research/product-options.md` (`0f5e90d`); принято направление A. Актуальный технический план — `PLAN.md`, контракт API и seed — `API_CONTRACT.md` (`4ba288b`). Матрица приёмки — `research/acceptance-matrix.md`.
- Seed-данные опубликованы в `seed/*.json` (`16ec791`); модель данных и модуль рейтинга с тестами — в `internal/model` и `internal/rating` (`c15a03a`); AI-модуль с mock-режимом и тестами — в `internal/ai` (`6395277`). Go API, Postgres-хранилище, seed-загрузчик и Docker опубликованы (`29411c9`); по отчётам Абылая и QA `go test`, Docker/Postgres и API e2e прошли, включая отклик на задачу с 30 баллами и пересчёт до 90 после подтверждения.
- React-поток опубликован (`7954407`). По отчёту frontend-исполнителя браузерный e2e с живым Go API прошёл: сырой текст → 5 mock-вопросов → правка карточки (preview 75→95) → подтверждение и 1-е место в каталоге → отклики → ручной выбор и отказ. Фильтр `industry=Культура&level=draft`, понятная 404 для несуществующей карточки и начисление 10 баллов команде за подтверждённый этап также проверены. `npm run build` и Docker build прошли.
- Продуктовый ресерч `research/icp-ux-decision-tree.md` завершён (`e12d0ee`, `bc2e70f`). Для демо предложен представитель МСП с конкретной операционной задачей и примером данных; это рабочая гипотеза, спрос не подтверждён.
- Обезличенные наблюдения из 12 уникальных клиентских звонков опубликованы в `research/voice-of-customer.md` (`51328ce`). Это качественный сигнал для UX, не доказательство поведения будущих пользователей платформы.
- Интервью Ильяса по бизнес-потоку и варианты первого проверяемого результата собраны в [`research/product-interview-2026-09-23.md`](research/product-interview-2026-09-23.md). Это продуктовый handoff для ограниченной параллельной работы Абылая, не подтверждение спроса клиентов и не смена обязательного демо-пути.
- Обзор 7 аналогов и 4 UX-паттернов опубликован в `research/analogues-ux.md` (`d194ae4`); применимые идеи касаются ручной правки AI-черновика, коротких вопросов, расшифровки рейтинга и структурированного отклика, без расширения MVP.
- Визуальный обзор публичных экранов и применимых приёмов опубликован в `research/analogues-visual-ux.md` (`76d5433`); точечные изменения первого экрана, CTA, шкалы 0–100 и формы отклика опубликованы в `web/**` (`8536d2c`). Сборка и браузерный e2e прошли; независимый QA на чистом `9de7833` дал cold 10s PASS по снимку 1280×720: роли, первый шаг и результат понятны, поле и кнопка видны без прокрутки. Остались низкие UX-риски у переключателя ролей и пояснения пользы для команд; оценка жюри автоматически не повышена.
- Дизайн-референс Qutty Band и границы переноса в Archibalt переданы владельцу UI/UX в [`research/design-reference-qutty.md`](research/design-reference-qutty.md); скриншот отправлен на Mac Абылая вне Git. Это handoff для точечных решений, без самостоятельных изменений `web/**` или API.
- SSH работает в обе стороны по ключам. Вход к Mac Ильяса разрешён только ключу Абылая с `10.19.24.78`; принудительная команда допускает чтение проекта, сообщения, выбранные Git-команды и `codex exec`. Абылай проверил ограничения и запуск `codex --version`.
- Сообщения Claude идут через `.agents/outbox.md` на Mac Абылая или `.agents/inbox.md` в этом проекте; задача Codex проверяет новые строки раз в минуту.
- Сценарий `demo/script.md` обновлён (`b8a0093`): первоначальный прогон на `4d41416` дал 55/#4 → 100/#1 → Qadam Data вошла по коду `000000` → отклик → ручное «Выбрана» за 71 с **без речи**. Демо-агент независимо повторил T0–T6 на более свежем `c4b1a94`, чистой БД `:18088` с обязательным входом: 5 вопросов → preview 45 → save 55/#4 → дополнение → save 100/#1 → вход Qadam Data → полный отклик → «Выбрана», UI PASS; тестовый стенд остановлен. T4 теперь разделяет рост 55→100 при сохранении и позицию #1 после подтверждения; текст полного пути — 144 слова (расчёт речи 62–69 с параллельно кликам). Сценарий остаётся draft до живого устного прогона.
- Критический регресс обязательного входа команды закрыт независимым QA на `46f8306`: отдельная новая Docker/Postgres DB `:18107`, `REQUIRE_TEAM_LOGIN=true`; raw → 5 mock-вопросов → preview 80 → ручная правка → 100 → confirm новой задачи, вход Jetki по контакту и коду `000000`, отклики, выбор/отказ и сохранение статусов после reload прошли. В UI больше нет выбора `team_id`; POST отклика без Bearer вернул 401. Это техническая приёмка версии до `b4e15ca`, не новая оценка жюри.
- README с запуском, архитектурой и тестовыми сценариями опубликован (`f4971af`). По отчёту Абылая `docker compose up --build` с нуля прошёл: SPA, health и 5 seed-задач.
- По независимому отчёту QA чистый изолированный Docker/Postgres-прогон прошёл: сборка, Go-тесты с БД, каталог с баллами задач 1–5 = 30/60/80/90/42, уровень priority на 1-м месте. README-формула совпала с кодом. На версии `372cb59` QA также провёл полный браузерный путь за 91 секунду: черновик → 5 mock-вопросов → 70→100 → публикация → два отклика → выбор/отказ → подтверждение этапа. Это время кликов, не устного выступления. Проверочный `:18080` находится на host QA.
- QA проверил `RecomputeRatings` (`55d384b`) на непустой БД: после ручного изменения балла и перезапуска восстановился рейтинг задачи, ID и отклик сохранились. Независимая проверка чистого клона с Docker прошла; техническая часть последней оценки — 10/10.
- Для защиты выбран Mac Ильяса. На нём поднят отдельный чистый демо-инстанс из `f5223ef` (код совпадает с проверенным `c41ea00`): `http://127.0.0.1:18089/`, контейнеры `archibalt-presentation-ilyas-v3-app`/`-db`, Docker network `archibalt-presentation-ilyas-v3`, Postgres без volume. При проверке в 14:35: health `ok`, БД доступна, web HTTP 200, AI mode `mock`, каталог 4=90, 3=70, 2=60, 5=42, 1=30; обязательный вход команды включён. Прежний собственный `:18084` остановлен; общий `:8080` и его volume не затронуты.

## Доступы и кредиты (без секретов)

Ваучеры активированы: OpenAI Platform показывает $50 Applied promotions; NVIDIA Brev показывает $50 Current Balance в разделе Pay For Compute; Ильяс активировал промомесяц ChatGPT & Codex Pro. Кредит Brev подтверждён для compute, но его применимость к NVIDIA API Catalog не подтверждена. Исследование вариантов — `research/credits-options.md`. Промокоды, API-ключи и скриншоты с открытыми кодами в Git не публиковать.

## Что блокирует следующий шаг

1. **Основной путь на последнем backend `17456c5` прошёл независимый browser QA** на чистой отдельной БД `:18110`, `REQUIRE_TEAM_LOGIN=true` по умолчанию, без HTTP-ошибок: сырой запрос → 5 mock-вопросов → preview 80 → правка до 100 → подтверждение задачи 6 → каталог #1 → вход Jetki по коду `000000` → авторизованный отклик → ручной выбор бизнеса → подтверждение этапа → reload; `stage_confirmed=true`, баллы команды 10, `go test ./...` PASS. Web не отправляет `owner_contact`, поэтому проверен открытый демо-путь задачи без владельца. Новая ветка входа бизнеса и WebSocket-чат через web не проверены; не заявлять их в демо без отдельного e2e. Если в показ добавят `owner_contact`, нужен новый браузерный QA на чистой БД. Предложено заморозить контракт критического пути до репетиции 17:00; решение Абылая ожидается.
2. На Mac Ильяса готов чистый стенд `:18089` версии `f5223ef`, проверены health/web/seed/AI mock; полный путь на этом конкретном стенде ещё не проходили, но независимый QA прошёл тот же код на отдельной чистой БД. Для репетиции в 17:00 засечь T0–T6 ≤5:00 и короткий путь ≤2:00 **с речью**, записать фактические баллы/позиции. После тестового прогона подготовить чистую БД для показа. Замер 71 с без речи не является PASS по времени.
3. Seed/rating на свежей БД дают 30/60/70/90/42 и четыре уровня. Сверить эти цифры с `demo/script.md`; на машине Ильяса Go runtime отсутствует, Docker-прогон доступен.
4. Внутренний Chair jury review №2 опубликован в `research/jury-review-2.md` (`59aac23`): **83/100, BORDERLINE → ближе к FINALIST**, технический отбор пройден, 10-секундный первый экран PASS на грани. Независимый QA на `f5223ef` дал отдельную предварительную оценку **87/100**, но тоже **NOT READY**: устный показ ≤5 минут не засечён. Оценки не заменяют друг друга и не являются финальным решением жюри. Rating QA: кейсы A/B/D/E PASS, C («Улучшим качество на 50%.») даёт 100 против экспертных 92; это качественное ограничение и продуктовый вопрос, не поломка обязательного e2e. Mock AI допустим по ТЗ; live OpenAI для защиты независимо не проверялся и не является обязательным техгейтом.
5. Общий локальный `:8080` использует старую непустую БД и не выбран для репетиции. Использовать только изолированный `:18089` на Mac Ильяса; перед защитой убедиться, что его БД чистая после репетиции.
6. UX-гейт Ильяса по первому экрану пройден независимым cold 10s QA на `9de7833` (1280×720). Для защиты остаётся проверить первый экран в реальном устном показе. В редакторе уже есть preview 0–100, 7 критериев и `+gain` для недостающих пунктов; суммарной плашки «было→стало +N» нет. До устной репетиции web-коммитов не планируется: рост 55→100 показывается двумя сохранениями и озвучкой. Плашку можно оценить только после репетиции, если останется время и QA.
7. QA заметил, что публичная карточка новой задачи показывает синтетический контакт бизнеса из поля карточки. Согласовать, какие контакты допустимо показывать публично; новый `owner_contact` в API маскируется. Backend не менять без продуктового решения.
8. **Функциональный stage gate закрыт независимым QA** после `6c2d7b9`: на отдельной чистой БД `:18109` с обязательным входом Jetki вошла по коду `000000`, отправила отклик, бизнес выбрал и нажал UI «Подтвердить этап»; после reload `stage_confirmed=true`, баллы 0→10. Повторный POST вернул 200 без двойного начисления; аналогичный seed-путь Bilim тоже дал 0→10, `go test ./...` PASS. Web-правка не потребовалась. `API_CONTRACT.md` и README синхронизированы с `selected`/`accepted` в `17456c5`. Этап из `selected` не означает двустороннее принятие проекта командой. Новый `17456c5` также поменял guards задачи и рейтинг; его критический браузерный путь перепроверяется отдельно.
9. По сообщению Абылая 15:32, он ждёт от Ильяса `owner_contact` в пяти seed-задачах, QA нового UI на стенде Ильяса и обновлённый `demo/script.md` под вход заявителя/динамический диалог/вход команды; решение по Brev тоже не передано. `AgentChat` с вариантами результата собран и проверен по API и в headless Chrome на живом API (`9c9261c`, `68d31ca`); независимый прогон на стенде Ильяса ещё нужен. Замечания по бэкенду с прогона: после `done` повторный `next-question` всегда возвращает `done`, поэтому «Спросить ещё» на живом API нового вопроса не даёт; сборка карточки `answers {}` занимает ~7 с. Замечание с API-прогона: `card_preview` в динамическом диалоге кладёт сырой ответ в поля, а сборка `answers {}` собирает карточку через AI, поэтому превью 47 может стать 34 при бессмысленных ответах; с осмысленными ответами разрыв меньше, в демо отвечать по делу.

## Обмен важными обновлениями

Полный протокол — `AGENT_HANDOFF.md`. После выбора идеи, завершения задачи, изменения владельца, появления блокера или важного коммита ответственный обновляет `STATUS.md` (либо передаёт координатору итог для него), коммитит и пушит результат, затем уведомляет другого агента короткой строкой с сутью и хешем коммита. Устный или SSH-апдейт не заменяет запись в Git.

- Ильяс → Claude: сообщение в `.agents/inbox.md` на Mac Абылая через существующий исходящий SSH-канал.
- Claude → Ильяс: сообщение в `.agents/outbox.md` на Mac Абылая либо в локальном `.agents/inbox.md` через ограниченный входящий SSH; Codex проверяет оба файла.
- Перед изменением общих файлов следовать правилам раздела «Работа в дуо» в `HACKATHON_RULES.md`; не переписывать чужую историю.

Следующий общий контрольный пункт: решение по спорному кейсу C рейтинга; на Mac Ильяса в 17:00 провести засечённый устный показ и повторить jury review.
